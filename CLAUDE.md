# 에이전트 작업 규칙

교회 청년부 회계용 **관리자 1인 전용** iOS 앱 + 청구 접수 워커.

**이 파일은 진실을 담지 않는다.** 여기 있는 건 "어느 문서를 언제 읽는가"와
"세션에서 무엇을 지키는가" 뿐이다. 구조·설계·규칙의 진실은 전부 사람이 읽는
문서에 있고, 이 파일은 거기로 보내는 라우터다.

> 그렇게 나눈 이유는 둘이다.
> ① 진실이 에이전트 전용 파일에만 있으면 사람이 안 읽는 곳에 지식이 고인다.
> ② 도구를 바꾸면 그 지식이 같이 사라진다.
> 그래서 **이 파일은 언제 지워도 프로젝트가 온전해야 한다.**

---

## 무엇을 언제 읽나

| 하려는 일 | 먼저 읽을 것 |
| --- | --- |
| 코드를 고친다 | [ARCHITECTURE.md](ARCHITECTURE.md) — 구조와 불변식 |
| 화면을 만들거나 고친다 | [DESIGN.md](DESIGN.md) — 시각 규칙 |
| 값의 근거가 궁금하다 | [TOSS.md](TOSS.md) — 참조한 시각 언어 원문 |
| 증상을 만났다 | [TROUBLESHOOTING.md](TROUBLESHOOTING.md) — 전에 겪은 것이면 원인이 거기 있다 |
| 설정·키가 필요하다 | [SETUP.md](SETUP.md) — 사람이 손으로 넣어야 하는 것 |
| 워커를 고친다 | [worker/README.md](worker/README.md) — 라우트·서명 |
| 프로젝트가 뭔지 모른다 | [README.md](README.md) |

---

## 세션 규칙

1. **고치기 전에 해당 문서를 읽는다.** 위 표대로. 건너뛰지 않는다.
2. **보수 작업을 하면 [TROUBLESHOOTING.md](TROUBLESHOOTING.md) 에 남긴다** —
   증상 · 원인 · 해결 · 재발 방지 네 가지로, 새 항목은 맨 위에.
   "이렇게 하세요"가 아니라 **"이래서 이렇게 됐다"** 를 적는다.
3. **구조를 바꿨으면 [ARCHITECTURE.md](ARCHITECTURE.md) 를 같이 고친다.**
   특히 "반드시 같이 고쳐야 하는 짝" 표에 항목이 늘거나 줄었으면 반드시.
4. **화면 코드에 숫자를 직접 적지 않는다.** `DS` 시맨틱 층만 쓴다.
5. **`@Published`/`@StateObject`/`ObservableObject` 쓰는 파일엔 `import Combine`.**
6. **커밋 메시지는 제목·본문 모두 한국어로 쓴다.**
7. **마이그레이션이 원격에 적용된 걸 확인하면 [SETUP.md](SETUP.md) 1번 표에 남긴다.**
   저장소만 봐서는 원격 스키마 상태를 알 수 없다 — 파일이 있다고 적용된 게 아니다.
   확인은 비밀번호 없이 된다 (anon key REST probe, SETUP.md 1번).

---

## 되돌리지 말 것

근거는 전부 [ARCHITECTURE.md](ARCHITECTURE.md) 에 있다. 여기서는 목록만 —
**"고쳐야 할 버그처럼 보이지만 아닌 것들"** 이다.

- 실시간 구독을 뷰의 `.task` 로 되돌리는 것 (SDK 채널 캐시 때문에 조용히 죽는다)
- `checkSession()` 을 `auth.session` 으로 되돌리는 것 (네트워크 없으면 로그아웃된다)
- `bills` 에 INSERT 정책을 여는 것 (검증이 두 군데가 된다)
- `PayeeViewModel` 을 탭마다 만드는 것 (계좌가 한쪽에만 보인다)
- 워커끼리 공개 URL 로 `fetch` 하는 것 (자기 자신으로 되돌아와 404)
- `updateStatus` 를 건별로 나눠 보내는 것 (끊기면 일부만 완료로 남는다)
- `balance` 를 저장 컬럼으로 되돌리는 것 (통장이 둘이라 어느 통장 잔액도 아니다 — 항목에서 유도한다)
- 농협 수기를 거래(`finance_transactions`)로 넣는 것 (은행 증명이 없어 **항목**이다 — `source=manual` 위장은 걷어냈다)
- 내부 이체를 두 줄로 적는 것 (짝이 깨지면 조용히 틀어진다 — `is_internal_transfer` 한 줄이 양쪽을 안다)
- 내부 이체 줄을 아예 안 넣는 것 (사람 장부엔 없지만 DB 엔 있어야 통장별 잔액이 나온다)
- 편집 잠금을 화면에만 두는 것 (은행 증명이 있는 항목은 `saveItemFields` 가 금액·일시·통장을 아예 안 보낸다)
- 내부 이체 판정을 하드로 잠그는 것 (`holder_name` 매칭이라 오탐이 날 수 있다 — 불러오기 확인 화면에서 사람이 끈다. 수기 항목은 이체일 수 없어 토글이 없다)
- 합계·보고서·그래프를 `filtered` 로 되돌리는 것 (`filteredExternal` 이라야 내부 이체가 빠진다)
- 거래내역서를 메일·Gmail API·별도 백엔드로 받으려는 것 (토스가 기기로 바로 주고 잠겨 있지도 않다)
- `onOpenURL` 을 화면에 다는 것 (찬 실행에서 탭이 아직 없어 조용히 사라진다 — 루트에서만 받는다)
- 거래내역서 확인을 시트로 되돌리는 것 (풀스크린이 상세를 push 로 받는다 — 시트 위엔 push 가 없다)
- 묶음 내용을 목록에 인라인으로 펴는 것 (목록이 길어져 손댈 줄이 밀린다 — 내용·합계 검산은 상세 몫이다)
- 헤더를 화면에서 손으로 그리는 것 (`AdminHeaderView` + `HeaderBackButton` 을 쓴다 — 손으로 그리면 아이콘 색·굵기가 갈라진다)
- 시트에 ✕ 닫기 버튼이나 헤더 타이틀을 다는 것 (시트는 그래버로만 닫는다 — 타이틀·버튼이 필요하면 풀스크린이다. DESIGN.md §1)

---

## 도구

- `wrangler` 는 전역 설치돼 있지 않다. `npx wrangler ...` 로 쓴다.
  배포 전 `npx wrangler deploy --dry-run` 으로 설정만 검증할 수 있다.
- `supabase db push --linked` 는 그냥 실행하면 CLI 가 임시 로그인 롤을 만들려다
  권한 오류로 실패한다. `SUPABASE_DB_PASSWORD` 를 넘겨 직접 접속해야 한다
  (SETUP.md 1번). **비밀번호는 사용자가 직접 입력하게 한다.**
- `xcode-select` 는 2026-08-21 에 Xcode 를 가리키도록 고쳤다. 빌드가 안 되면
  `xcode-select -p` 부터 확인한다. 빌드 명령은 [ARCHITECTURE.md](ARCHITECTURE.md#빌드).
- **화면 확인과 푸시 확인은 사람이 해야 한다.** 시뮬레이터는 Google 로그인 벽에서
  막히고, 인증은 대신 진행하지 않는다.

---

## 지금 상태

- 스키마 전환 · 계좌부 · 공개 폼 · 푸시 · 묶어 보내기 · 소비 분석까지 **실기기에서
  확인됐다.** (이전 상태)
- 2026-09-05~08 에 **재정 2층 스키마 재설계**를 했다 — 거래(`finance_transactions`)=
  은행 증명, 항목(`finance_items`, 구 `finance_splits`)=장부 정본. 잔액·보고서·목록이
  전부 항목에서 나온다. 근거는 [ARCHITECTURE.md](ARCHITECTURE.md) "통장은 둘, 장부는
  하나". **2026-09-10 에 헤더 통일까지 묶어 `main` 에 병합했다(PR #1). 작업 브랜치
  `finance-two-layer-schema` 는 병합 후 삭제.**
  - 원격은 마이그레이션 적용 + 엑셀 재삽입(2025.01~2026.07 농협 273건)까지 **끝났다.**
  - 편집을 `ItemEditView` 하나로 통합(필드 시트 즉시 저장), 선택 모드 3-액션
    (병합·삭제·카테고리), 거래 추가를 토스식 풀스크린·커스텀 키패드로 다시 짰다.
  - **거래내역서 확인(`StatementImportView`)을 레퍼런스 스타일로 재설계**했다 —
    풀스크린, 입금/출금 칩. **목록이 "생성된 청구 내역"(항목별, 영수증 썸네일)**이고
    행을 누르면 **청구 정보/영수증 탭 상세(`StatementItemDetailView`)** 로 push:
    ① 청구 내역·금액 ② 청구 정보(카테고리 없으면 '없음'+셰브런 입력) ③ 매칭된 은행
    거래+포함 청구 카드+합계 검산. **카테고리를 항목별로 승격**(`StatementMatch.itemCategories`,
    구 `manualCategory` 대체). 초록 빌드. DESIGN.md §13 에 문서화된 예외로 적었다
    (색 시스템·묶음 보라 `DS.Palette.group`). 8월 입력으로 실기기에서 확인됨.
- **2026-09-10 이후 `main` 에 더 들어간 것** (커밋 이력) — 공개 폼 보안(Turnstile
  사람 확인 세션 패스, IP당 요청 제한, R2 고아 영수증 정리), 재정 탭 카테고리 아이콘,
  피쳐별 README 규약·템플릿, 매칭 문서를 Import 폴더로 이동. 실기기 확인 여부는 미표기.
- **2026-09-11 문서 정리** (코드 변경 없음) — README 를 개요 중심으로 줄였다
  (해결하려는 문제·핵심 기능·전체 동작 흐름·프로젝트 구조). 도메인·데이터 흐름·
  비즈니스 로직·왜 토스뱅크는 [ARCHITECTURE.md](ARCHITECTURE.md) 로 옮기고 README 는
  링크만 건다. ARCHITECTURE 에 "왜 토스뱅크 모임통장인가"(설계 근거)와 "도메인 단위"
  (청구·송금 묶음·거래·항목, `BillGroup` 은 표가 아니라 `processed_at` 로 재구성)를
  새로 넣고, 계좌를 청구자에게 안 받는 이유를 데이터 흐름 절에 살렸다.
- **남은 일:** ① ~~8월 데이터 넣기~~ **끝.** 2026-09-10 에 앱에서 농협 수기 + 토스
  내역서 불러오기로 8월분을 정상 입력했다 — 재설계 화면들(거래 추가·불러오기 흐름·
  항목 상세·선택 모드·필드 편집)이 이때 실기기에서 실전으로 돌아갔다.
  ② 실기기 확인 — 위 흐름은 8월 입력으로 확인됨. 아직 안 본 건 다크 모드·큰 글씨뿐.
  ③ ~~브랜치 → `main` 병합~~ **끝.** 2026-09-10 PR #1 로 병합, 브랜치 삭제.
  ④ 분할 재편집·이체 사후 토글 UX(⑦, `ItemEditView` 에 아직 없다).
  ⑤ ~~불러오기 화면 헤더 통일~~ **끝.** 2026-09-10 에 헤더 문법을 둘로 정리했다 —
    **시트는 그래버만**(타이틀·버튼 없음), **타이틀·버튼이 필요하면 풀스크린 +
    `AdminHeaderView`**. 뒤로 가기는 공용 `HeaderBackButton` 하나로 통일하고, 손으로
    그리던 `‹` 헤더(불러오기 상세·편집)와 거래 추가의 시스템 내비바를 걷어냈다.
    상세 시트(`BillDetailView`·`AccountBalanceView`·`SpendingDetailView`)의 ✕ 는 뗐다.
    근거·규칙은 [DESIGN.md](DESIGN.md) §1 "시트냐 풀스크린이냐". 초록 빌드. 8월 입력으로 실기기에서 확인됨(다크·큰 글씨 제외).
  ⑥ ~~편집 폼 시트 풀스크린 이관(A그룹)~~ **끝.** 2026-09-10 에 시스템 내비바를 쓰던
    편집·목록 화면을 전부 풀스크린 + `AdminHeaderView` 로 옮겼다(항목 상세·필드 편집·
    프로필·계좌·카테고리 붙이기·영수증 관리·보고서 미리보기·알림함·영수증 뷰어).
    **앱에 `navigationTitle` 은 이제 없다.** 남은 시트는 그래버형 콘텐츠 시트뿐이다.
    선택 모드 취소도 원형 `HeaderCancelButton` 으로 통일. 초록 빌드. 8월 입력으로 실기기에서 확인됨(다크·큰 글씨 제외).
- 안 본 것은 여전히 **큰 글씨(Dynamic Type)** 와, 다크 모드의 **상태 배지·보고서
  미리보기**다 ([DESIGN.md](DESIGN.md) 맨 아래).
