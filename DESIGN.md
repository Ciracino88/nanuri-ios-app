# 디자인 가이드라인

이 문서는 **이미 앱에 있는 규칙을 적은 것**이지 새로 정한 취향이 아니다.
값은 전부 `NanuriAdmin/Components/DesignSystem.swift` 의 `DS` 에 이름으로 들어 있고,
원시 팔레트는 `DesignTokens.swift` 의 `Ramp` 에 따로 있다.
**화면 코드에 숫자를 직접 적지 않는다.** 필요한 값이 없으면 먼저 `DS` 에 이름을 붙인다.

시각 언어의 출처는 저장소 루트의 **`TOSS.md`** 다 (`getdesign.kr` 카탈로그 원문
그대로). 값을 다시 볼 일이 있으면 거기가 근거고, 이 문서는 **그 값을 이 앱에서
어떻게 쓰기로 했는지**를 적는다. 둘이 어긋나면 이 문서가 이긴다 — 원문은 금융
슈퍼앱의 것이고 우리는 회계 장부다.

관리자 1인이 쓰는 앱이고 화면마다 정보 밀도가 높다. 아래 규칙들은 "예뻐 보이려고"가
아니라 **한 화면에 많은 정보를 넣고도 읽히게** 하려고 있다.

> **벤더 중립.** 시각 언어는 빌려 왔지만 그 브랜드 이름은 우리 UI 어디에도 넣지
> 않는다 — 카피·라벨·헤더·타입 이름 전부. `TossResultView`·`TossTransfer` 의
> "토스"는 **실제 송금 딥링크 연동**(`supertoss://send`)이라 예외다. 그건 디자인
> 출처가 아니라 우리가 실제로 호출하는 앱의 이름이다.

---

## 0. 이 시스템의 성격

세 문장으로 줄이면 이렇다.

1. **평면이 기본이다.** 그림자는 떠 있는 것(메뉴·다이얼로그·토스트·바닥 고정 바)에만
   쓴다. 목록 카드에는 그림자도 테두리도 없다.
2. **강조색은 화면당 하나다.** 파랑은 그 화면의 가장 중요한 행동이 독점한다.
3. **빨강은 아껴 둔다.** 되돌릴 수 없는 것에만 쓴다 — 출금은 빨강이 아니다.

---

## 1. 화면의 뼈대

```
화면 배경 DS.Surface.page (회색) 또는 .card (흰색) — 안전영역까지
├─ 헤더 AdminHeaderView   배경 없음 — 화면 배경 위에 그냥 얹힌다
└─ 카드 또는 섹션
```

**헤더에 배경색도 구분선도 주지 않는다.** 목록이 헤더 아래로 지나가지 않으므로
(같은 `VStack` 안이다) 경계를 그릴 이유가 없다. 상단에 색이 다른 띠가 하나 더
생기면 화면이 그만큼 낮아 보인다. 그래서 `screenBackground()` 는 안전영역까지
덮는다 — 안 그러면 상태바 자리에만 다른 색이 남는다.

### 화면 전체가 한 덩어리로 스크롤된다

**고정으로 남는 건 헤더뿐이다.** 필터 줄·요약·날짜 축이 목록과 같이 굴러간다.

머리 콘텐츠를 헤더 아래에 붙박아 두면 목록이 좁은 창처럼 보인다. 재정 탭이 그랬다 —
화면 위 250pt 가 고정이라 남은 자리에서만 훑어야 했다.

- 스와이프 동작이 **없는** 화면은 `ScrollView` + `LazyVStack` (재정 탭)
- 스와이프 동작이 **있는** 화면은 `List` 를 유지하고, 필터 줄을 **첫 행으로** 넣는다
  (청구서 · 계좌부). `ScrollView` 로 갈아엎으면 계좌부의 스와이프 삭제가 사라진다 —
  거기엔 삭제 버튼이 따로 없어서 길게 누르기 하나만 남는다.

### 카드냐 섹션이냐

둘은 다른 문법이다. **섞어 쓰지 않는다.**

| | 카드 | 섹션 |
| --- | --- | --- |
| 생김새 | 흰 면 + radius 24, 회색 배경 위 | 배경 위에 줄이 그냥 앉음 |
| 가르는 것 | 카드 경계 | 여백 (또는 헤어라인) |
| 쓰는 곳 | 항목 하나가 독립된 덩어리일 때 (청구서 · 계좌부 · 장부) | 훑어 읽는 목록 (재정 거래 · 알림함) |

**훑는 화면에 상자를 주지 않는다.** 항목마다 테두리가 생기면 화면이 같은 크기 상자의
반복이 되고, 눈이 내용이 아니라 상자를 센다. 재정 탭 거래 목록과 알림함이 섹션인
이유다.

카드는 `.cardStyle()`, 카드 **안에서** 줄을 가를 때는 `.cardRowDivider()` 다.
여러 줄을 담는 카드는 `cardStyle(padding: 0)` 으로 받고 안쪽 여백은 행이 갖는다 —
상자가 여백을 가지면 그 테두리 안쪽이 안 눌린다.

### 헤더 (`AdminHeaderView`)

**모든 탭이 이 헤더를 쓴다.** 내비게이션 바는 전부 걷어냈다 — `NavigationView` +
`.navigationTitle` 을 섞어 쓰던 시절엔 탭마다 상단 높이도 제목 크기도 달랐다.

| 자리 | 내용 |
| --- | --- |
| 왼쪽 | 화면별 동작 하나 (`leading:`). 없으면 빈다 |
| 가운데 | **탭 이름**, 또는 화면이 넘겨받은 것 (`center:`) |
| 오른쪽 | 화면별 동작 하나 (`trailing:`) |
| 맨 오른쪽 | 알림 (헤더가 직접 갖는다) |

- 알림만 어느 탭에서나 같은 자리에 있어야 해서 **화면이 넘기지 않는다.**
- **새로고침 버튼은 없다. 목록을 아래로 당겨서 새로고침한다** (`.refreshable`).
  비워진 왼쪽은 화면이 쓴다 — 청구서 탭의 묶어 보내기 선택 모드.
- **빈 화면에서도 당길 수 있어야 한다.** `.refreshable` 은 스크롤되는 것에만 붙으므로
  `EmptyStateView` 는 `.pullToRefresh { }` 로 감싼다.
- 한쪽에 아이콘은 **하나까지**. 둘 이상이면 `Menu` 하나로 묶는다 (재정 탭의 `⋯`).
- **가운데는 내줄 수 있다.** 탭 이름은 탭바가 이미 말하고 있어서, 그 자리가 더
  쓸모 있는 화면이면 넘긴다 — 재정 탭은 달 넘김(`‹ 2026년 7월 ›`)이 거기 있다.
  **대신 화면이 무엇인지 다른 데서 반드시 말해야 한다.**
- `titleAction` 을 주면 이름 옆에 `chevron.down` 이 붙는다. **▾ 는 펼쳐진다는
  뜻이지 뒤로 간다는 뜻도, 새로고침한다는 뜻도 아니다.** 한동안 여기에 "게이트
  화면으로 되돌아가기"가 걸려 있었고, 그 뒤엔 게이트 화면이 새로고침을 걸고
  있었다. 둘 다 화살표만 보고는 알 수 없어 눌러 봐야 했다.
  **지금은 `titleAction` 을 쓰는 화면이 없다** — 장부 전환이 마지막이었다.
- 안 읽은 알림은 **주황 점** 하나로만 알린다. 빨강도 파랑도 아닌 이유는 5번에 있다.
- **아이콘뿐인 버튼에는 `accessibilityLabel` 을 반드시 준다.** `HeaderIconButton` 이
  `label` 을 필수 인자로 받아서 빠뜨릴 수 없게 해 뒀다.

내비게이션 바가 없으므로 `.searchable` 도 못 쓴다. 검색은 `SearchField` 를 놓는다.

`Form` 은 **입력 화면에서만** 쓴다 (`TransactionEditView`, `PayeeEditView`).
목록에는 쓰지 않는다 — 카드·섹션 구조와 섞이면 배경이 두 가지가 된다.

### 카드는 정보, 결정은 시트

**목록 카드에 액션 버튼을 넣지 않는다.** 카드는 읽는 것이고, 누르면 열리는 시트가
그 항목으로 할 수 있는 일을 전부 갖는다.

청구서 카드에는 원래 36×36 버튼이 네 개(영수증·송금·거절·삭제) 금액 옆에 붙어
있었다. 두 가지가 잘못됐다 — **송금과 거절이 6pt 간격으로 나란히 있었고**, 글씨를
키우면 그 줄부터 무너졌다. 목록에서 한 번에 되는 편의보다 잘못 누르지 않는 게 우선이다.

**예외는 선택 모드 하나뿐이다.** 청구서 탭 헤더 왼쪽의 `checklist` 를 누르면 카드가
"눌러서 고르는 것"이 된다. 이때 이니셜 원 자리에 체크가 들어오고 오른쪽 화살표가
빠진다 — 눌러도 시트가 안 열리는데 화살표를 두면 거짓말이 된다. 선택은 **모드에
들어가야만** 생기고 나올 때 사라진다.

이니셜 원(`InitialAvatarView`)은 줄을 잡아 주는 표식일 뿐이라 **사람마다 색을
다르게 주지 않는다.** 색은 뜻이다 (5번).

### 상세 시트는 위에서 아래로 한 줄기다

```
                              ✕     ← 오른쪽 위. 닫는 자리가 눈에 보인다
        198,000원                   ← 금액(h1). 제일 먼저 읽는 것
     아직 송금하지 않았어요             ← 상태를 문장 한 줄로 (색이 뜻이다)
  ─────────────────────────         ← 머리와 값을 가르는 선 한 올
  이름                    홍길동
  항목                 3월 간식비      ← 상자 없이, 줄 사이만 넓게
  청구일        2026년 3월 9일 오전 10시
  계좌            국민 123456-78-901
  [ 🗎  영수증 보기 ]                 ← 결정이 아닌 것
  ─────────────────────────
  [ 거절 ]        [ 송금하기 ]        ← 바닥 고정
```

- **✕ 는 오른쪽 위**다. 손잡이로도 내릴 수 있지만 *닫는 자리가 눈에 보이는 것*과
  *아래로 끌 수 있는 것*은 다른 이야기다.
- **상태는 칩이 아니라 문장**이다. 목록에서는 여러 건을 훑으므로 배지로 줄이지만,
  시트는 한 건만 들여다보는 자리다. 문구(`statusSentence`)와 색(`statusTone`)을
  `Bill` 한 곳에 두어야 두 화면이 안 갈라진다.
- **값 줄에는 선도 상자도 없다.** 줄 사이를 벌리면 라벨-값 짝이 가로로 읽힌다.
  머리와 값 사이의 선 한 올만 남긴다 — 없으면 값 네 줄이 금액에 딸린 설명처럼 붙는다.
- **아이콘이 붙는 버튼은 결정이 아닌 것 하나뿐이다.** "영수증 보기"의 아이콘이
  "이건 잠깐 보고 돌아오는 것"이라고 말해 준다.

### 시트 높이는 화면 비율로 고정한다

내용을 재서 딱 맞추던 시절이 있었는데 **열 때마다 높이가 달랐다** — 첫 측정이
애니메이션 중 어느 순간에 걸리느냐를 탔다 (`TROUBLESHOOTING.md`).
지금은 `.fraction(DS.Sheet.billDetail)` 이다. **늘 같은 자리에서 열리는 게, 매번
딱 맞지만 매번 다른 것보다 낫다.** 되돌리지 말 것.

비율은 내용보다 **조금 넉넉하게** 잡는다. 남으면 스크롤 안쪽 아래에 빈 자리가 생길
뿐이지만, 모자라면 버튼이 잘린다.

### 같은 값을 한 화면에 두 번 적지 않는다

**한 화면에 같은 값이 두 번 있으면 어느 쪽을 봐야 하는지 읽는 사람이 판단해야 한다.**
`BillDetailView` 머리에서 이름·계좌·상태를 값 줄로 내리고 금액과 상태 한 줄만 남긴
것이 그 결과다.

같은 이유로 **목록 행에서 뺀 값은 상세에 있어야 한다.** 거래 행에서 잔액을 빼면서
`TransactionEditView` 에 "거래 후 잔액"을 새로 넣었던 게 그 예다 — 빼기만 하면
옮긴 게 아니라 없앤 것이다.

**그 "거래 후 잔액"은 지금 없다.** 통장이 둘이 되면서 한 줄의 잔액이 어느 통장의
잔액도 아니게 됐다. 그 자리는 **어느 통장인지**가 대신 채우고, 잔액 자체는
통장 화면(`AccountBalanceView`)으로 옮겨 갔다 — 한 거래에 딸린 값이 아니라
통장에 딸린 값이라서다.

---

## 2. 여백

4px 를 기본 단위로 하는 12단 사다리다 (`DS.Spacing.s1` ~ `s20`).

뜻이 붙은 이름이 있으면 그쪽을 쓴다.

| 이름 | 값 | 자리 |
| --- | --- | --- |
| `tight` | 4 | 밀접하게 붙는 것들 (라벨과 입력) |
| `small` | 8 | |
| `medium` | 12 | |
| `screen` | **24** | 화면 좌우 여백. 원문이 값을 발행한 자리다 |
| `section` | 32 | 섹션 사이 |
| `sheetEdge` | 32 | 시트 안쪽 위아래 |
| `cardGap` | 8 | 세로로 이어지는 카드 사이 (위아래 각각) |

**섹션 사이는 넉넉히 벌린다.** 칩 → 요약 → 날짜 축 → 목록이 12pt 씩 붙어 있으면
어디까지가 한 덩어리인지 안 보인다. 지금은 20~24pt 다.

**스크롤 안 마지막 버튼과 바닥 바 사이는 `sheetEdge`(32)만큼 띄운다.** 붙어 있으면
위 버튼을 누르려다 아래를 짚는다.

---

## 3. 모서리

4~32px 8단 + `Capsule`. **이 밖의 임의 값을 쓰지 않는다.**

| `DS.Radius` | 값 | 자리 |
| --- | --- | --- |
| `xs` `s` | 4 · 8 | 작은 배지 · 인라인 태그 |
| `m` = `control` | 12 | 입력 필드 · 세그먼트 트랙 · 아이콘 타일 |
| `l` | 14 | 시트 안 큰 버튼 · 목록 행 아바타 |
| `xl` | 16 | 화면 최하단 고정 버튼 |
| `xl2` = `group` | 20 | 시트 · 다이얼로그 |
| `xl3` = `card` | **24** | 카드 |
| `xl4` | 32 | hero 블록 |
| `button` | 10 | 정사각 아이콘 버튼 |

**카드는 24다.** 16(버튼 라운드)으로 두면 야무져 보여서 이 시스템 특유의 뭉툭함이
안 난다. pill·원형은 고정값이 아니라 `Capsule()` 이다.

**버튼은 높이와 모서리가 짝으로 움직인다** — XL 56/16, L 48/14, M 40/12, S 32/10.

---

## 4. 글자

`DS.Typo` 가 크기·굵기·행간·**자간**을 한 덩어리로 들고 다닌다. `.typeStyle(_:)` 로 건다.

**자간이 음수다.** 큰 글자일수록 조여 무게를 잡는 것이 이 시스템의 특징이라
자간을 토큰에 같이 넣었다 (h1 은 −0.56pt).

| 무리 | 단계 | 쓰는 곳 |
| --- | --- | --- |
| Display · Heading | `h1`(28) `h2`(24) `h4`(20) | 시트 머리 금액 · 로그인 제목 · 밴드의 큰 수 |
| Title | `title1`(18/600) `title2`(17/600) | 헤더의 달 · 거래 행 금액 |
| Body | `body1`(17) `body2`(15) `body3`(13) | **본문 기본은 `body2`(15/1.5)** |
| Label | `labelL`(17/700) `labelM`(15/600) `labelS`(13/600) | 버튼 · 칩 · 목록 행 제목 |
| 금액 | `amount`(15/700) | 목록 행 금액 |
| Caption | `caption`(12) `captionS`(11) | 날짜 셀렉터 · 배지 |

역할 이름이 붙은 것들 (`rowTitle()`, `rowSubtext()`, `heroAmount()` …)이 있으면
그쪽을 쓴다. 크기를 화면마다 고르기 시작하면 사다리가 갈라진다.

### 목록 행은 15pt 다

**제목과 금액이 같은 15pt 이고 굵기만 갈린다** (Semibold ↔ Bold).

18(`title1`)이었던 적이 있는데 그건 원문이 **카드 타이틀**에 준 값이지 촘촘히
반복되는 목록 행의 값이 아니다. 행에서 제목이 금액보다 커지면 무게 중심이 왼쪽으로
쏠린다 — 목록에서 먼저 읽혀야 하는 건 금액이다.

### 금액은 tabular figure 로

`.tabularAmount()` 를 건다. 목록에서 금액이 세로로 쌓일 때 숫자 폭이 제각각이면
자릿수가 어긋나 보인다. **`typeStyle()` 뒤에 붙인다** — 폰트를 나중에 걸면 덮인다.

### 좁은 자리에서는 금액을 줄인다

`Int.compactAmount` — `892,500` → `89.3만`.

- **만 미만은 안 줄인다.** `0.85만` 은 줄인 게 아니라 읽기만 어려워진다.
- **소수는 첫째 자리까지만.** 자릿수가 늘면 결국 다시 축소돼서 도로아미타불이다.
- **`.0` 은 뗀다.** `50.0만` 이 `50만` 보다 정확해 보이지만 주는 정보는 같다.

정확한 수가 필요한 자리에는 쓰지 않는다. 날짜 셀렉터 칸처럼 **크기를 가늠하는
자리**에만 쓴다.

---

## 5. 색

색은 **장식이 아니라 뜻**이다. 화면이 달라도 같은 뜻이면 같은 색이어야 한다.

화면은 시맨틱 층(`DS.Ink` · `DS.Surface` · `DS.Line` · `DS.Palette` · `DS.Tone`)만
쓴다. **원시 팔레트(`Ramp`)를 직접 참조하지 않는다** — 새 role 을 만들 때만 본다.

| `DS.Palette` | 색 | 뜻 |
| --- | --- | --- |
| `accent` = `deposit` | 파랑 | 주요 동작 · 입금 · 첨부 |
| `withdrawal` | **검정** | 출금 |
| `danger` | 빨강 | 되돌릴 수 없는 것 (삭제 · 거절 · 로그아웃), 잔액이 음수인 상태 |
| `pending` | 주황 | 대기 · 주의 (계좌 미등록) |
| `done` | 초록 | 완료 (송금됨) |

### 금액의 색은 셋이다

| | 색 | 왜 |
| --- | --- | --- |
| 입금 | 브랜드 파랑 (`Palette.deposit`) | 들어온 돈 |
| 출금 | 본문 검정 (`Palette.withdrawal`) | 나간 돈. 아래 참고 |
| **통장 사이 이체** | **회색 (`Ink.tertiary`)** | **수입도 지출도 아니다** |

내부 이체는 장부 전체로 보면 나간 돈도 들어온 돈도 아니고 합계·보고서에서도
빠진다. 파랑이나 검정을 주면 **그 줄이 다른 줄과 같은 종류의 수로 읽힌다** —
400만원짜리 줄이 그 달의 제일 큰 수입처럼 보인다.

거래내역서 확인 화면도 같은 규칙을 쓴다. 넣기 전에 그 성격이 보여야 한다.

### 출금이 빨강이 아닌 이유

원문 `list-row` 규칙이 **양수는 브랜드색, 음수는 본문색**이다. 빨강이 아니다.

장부에서 지출은 사고가 아니라 일상이다. 출금을 빨강으로 칠하면 목록 절반이
경고처럼 보이고, 정작 진짜 경고(거절·삭제)가 묻힌다.

### 강조색은 화면당 하나다

파랑은 **그 화면의 가장 중요한 행동**이 독점한다. 필터 칩 선택색이 파랑이 아니라
검정인 이유가 이것이다 — 칩까지 파랗게 칠하면 강조가 둘이 된다.

같은 이유로 안 읽은 알림 점은 주황이다. 파랑이면 주요 동작과 섞이고, 빨강이면
되돌릴 수 없는 것과 섞인다.

**예외 하나** — 요약 밴드의 전월 대비 문구와 그래프는 빨강/파랑을 쓴다
(더 썼으면 빨강, 덜 썼으면 파랑). 견주는 자리라 색이 없으면 문장이 그냥 흘러가고,
지출이 늘어난 건 되돌릴 수 없는 일이 맞다.

### 옅은 바탕은 알파로 만들지 않는다

`DS.Tone` 이 **글자색과 옅은 바탕을 짝으로** 들고 있다. `color.opacity(0.12)` 로
만들던 자리가 전부 이걸로 바뀌었다.

배지의 washed 단계는 원문에 없어서 **이 앱의 추정치**다. 코드에 그렇게 적어 뒀다.

무채색 알파(`State.pressOverlay` · `State.scrim` · `Line.subtle`)는 예외다 —
그쪽은 원문이 토큰으로 발행한다.

### 눌림과 비활성

- 눌림은 **검정 26% 덮기** (`State.pressOverlay`). 그림자도 색 교체도 아니다.
  `PressOverlayButtonStyle` 이 처리한다.
- 비활성은 **노드 전체에 30% 불투명도** (`State.disabledOpacity`).
  부분 회색 처리하지 않는다.

---

## 6. 아이콘

**굵기를 `.regular` 로 고정한다.** `DS.Icon.font(_:)` 만 쓰고 `weight:` 를 직접
넘기지 않는다.

원문 stroke 가 24px 그리드에서 1.5~1.67px 인데, SF Symbols 의 `.medium`/`.semibold`
는 그보다 훨씬 두껍다. 같은 크기여도 선이 굵으면 화면이 무거워 보인다.

| `DS.Icon` | 값 | 쓰는 곳 |
| --- | --- | --- |
| `s` = `inline` | 16 | 인라인 · 작은 셰브런 |
| `m` | 20 | 목록 셰브런 · 입력 필드 |
| `l` = `action` = `feature` | **24** | 헤더 · 목록 행 대표 아이콘. 일꾼이다 |
| `xl` | 32 | |
| `placeholder` | 48 | 빈 화면 |

---

## 7. 그림자

**평면이 기본이다.** 역할 이름으로만 고른다 (`.elevation(_:)`).

| `DS.Shadow` | 쓰는 곳 |
| --- | --- |
| `menu` | 세그먼트에서 선택된 칸 |
| `tooltip` | |
| `dialog` | 화면을 덮는 판 |
| `toast` | |
| `bottomBar` | 바닥 고정 바. **위로만 던진다**(y 음수) |

**카드에는 그림자도 테두리도 없다.** 흰 카드와 회색 배경의 명도 차만으로 경계를
만든다. 한때 `line-subtle` 헤어라인을 둘렀는데, 카드마다 선이 생기니 화면이 목록이
아니라 **폼처럼** 읽혔다. 토큰에 그 용도가 적혀 있어도 실제로는 안 쓰는 자리다.

**inner shadow 는 없다.** 눌린 상태는 그림자가 아니라 overlay 다.

---

## 8. 컨트롤

- **목록을 거르는 컨트롤은 `ChipSelector` 하나다.** 개수를 같이 보여줘야 해서
  `Picker(.segmented)` 를 쓰지 않는다. 칩은 왼쪽부터 채우고 넘치면 가로로 스크롤한다.
  선택된 칩은 **검정으로 채우고** 글자를 뒤집는다. (`PillPicker` 는 남아 있지만
  재정 탭이 칩으로 옮겨 가면서 쓰는 곳이 없다.)
- **시트의 큰 버튼**은 `ActionButton`. 높이 48(L), radius 14, 글자 `labelL`.
  종류는 넷 — `primary`(파랑 채움) · `secondary`(회색 채움) · `tinted`(투명 + 파란
  글자) · `destructive`(빨강 채움).
  나란히 둘 때는 **되돌릴 수 없는 쪽을 왼쪽에** 놓는다. 오른손 엄지가 먼저 닿는
  자리가 오른쪽이다. 남은 동작이 하나뿐이면 **가로를 다 쓴다**.
- **화면 최하단 강제 액션은 `BottomCTA`** (56pt, radius 16). 위쪽에 흰색→투명
  보호 그라디언트를 깐다. **`ActionButton(.primary)` 와 같은 화면에 같이 두지
  않는다** — 둘 다 "이 화면의 가장 중요한 행동"을 주장해서 강조점이 흩어진다.
  이 앱에서 그라디언트가 허용되는 자리는 여기뿐이다.
- **아이콘은 결정이 아닌 버튼에만 붙인다.** 오른쪽에 붙이는 자리는 만들지 않는다 —
  원문이 suffix icon 을 Arrow 계열로만 허용하는데, 이 버튼이 여는 것은 다음 화면이
  아니라 결정이다.
- **바닥에 고정하는 바는 `.elevation(.bottomBar)`** 로 위로만 그림자를 던진다.
  헤더에 구분선을 안 주는 것과 어긋나 보이지만 이유가 정반대다 — 헤더는 목록이
  그 아래로 지나가지 않아서 그릴 경계가 없고, 고정된 버튼 위는 **내용이 실제로
  거기서 잘린다.**
- 되돌릴 수 없는 동작은 **확인을 한 번 받는다** (`alert` / `confirmationDialog`).

---

## 9. 움직임

**바운스 오버슈트가 없다.** 스프링을 쓰지 않는다.

| | 값 |
| --- | --- |
| `DS.Motion.fast` | 120ms — 버튼 눌림 |
| `DS.Motion.base` | 200ms — 토글 · 선택 변경 |
| `DS.Motion.slow` | 320ms — 시트 · 다이얼로그 |
| `DS.Motion.ease(_:)` | 기본 곡선 (ease-out-expo) |
| `DS.Motion.easeOut(_:)` | 더 빠르게 떨어지는 곡선 |

`list` · `control` · `sheet` 는 위 조합에 이름을 붙인 것이다. 새 곡선을 만들지
않는다 — 화면마다 미묘하게 다른 속도는 눈에 띄지 않으면서 앱을 조잡하게 만든다.

**skeleton shimmer 를 쓰지 않는다.** 로딩은 `ProgressView` 다.

---

## 10. 빈 상태

`EmptyStateView` 를 쓴다. 아이콘은 선택이고, **무엇을 하면 채워지는지**를 적는다.

**목소리를 낮춘다** — 제목은 15 semibold 회색, 설명은 13 더 옅은 회색이다.
18 semibold 에 본문 검정이던 적이 있는데, 아무것도 없는 화면에서 그 한 줄만 크고
진하니 "없다"가 아니라 "무슨 일이 났다"로 읽혔다. **없는 건 사건이 아니라 상태다.**

**비어 있는 이유가 여럿이면 문구도 여럿이어야 한다.** 재정 탭은 셋으로 갈린다 —
고른 날이 비었나, 거르개가 비었나, 이 달이 통째로 비었나. 날짜 필터가 켜진 채로
"다른 달을 보세요"를 띄우면 엉뚱한 데를 가리킨다.

---

## 11. 말투

- 화면의 모든 글은 **한국어 해요체**다. "저장되었습니다"가 아니라 "저장했어요".
  **잘못이 우리 쪽인 오류에서도 해요체를 유지한다.**
- 오류 메시지의 역할은 **다음으로 안내하는 것**이지 멈춰 세우는 것이 아니다.
  `error.localizedDescription` 을 그대로 올리는 건 마지막 수단이다.
- 버튼은 **일어날 일을 직접 말한다** — `송금하기`, `다음`. "여기를 눌러주세요" 같은
  지시형은 쓰지 않는다.
- 챗봇 톤("~해보세요!")과 마케팅 과장("혁신적", "최고의")을 쓰지 않는다.
- 금액은 `1,000원` 처럼 세 자리 구분 + `원` 을 **공백 없이** 붙인다.
- 날짜는 한국식이다 — 0 을 생략하고(`7월 4일`), **요일에 괄호를 두르지 않는다**
  (`28일 화요일`).
- 문장 안 금액은 만 단위로 줄인다 (`144만원`). 문장은 정확한 수를 읽는 자리가
  아니라 크기를 가늠하는 자리다.

---

## 12. 재정 탭의 구성

이 앱에서 화면 구조가 가장 많이 손질된 자리라 따로 적는다. 위에서 아래로
**범위가 좁혀지는** 순서다.

```
헤더        [＋]  ‹ 2026년 7월 ›  [분류] [≡]   ← 왼쪽 만들기 · 오른쪽 관리
칩 셀렉터    전체 19 · 입금 5 · 출금 14 · 미분류 12
요약 밴드    총 입금 / 총 출금          ← 전체 폭 회색 띠, 카드 아님
            지난달보다 144만원 더 나갔어요  [그래프]
            자세히 보기 ›
날짜 셀렉터   일 월 화 수 목 금 토        ← 주 단위로 페이징
            26 27 28 29 30 31  1
정렬 줄                          최신순 ▾   ← 오른쪽 끝, 목록 바로 위
섹션 목록    26일 일요일
              +85,000원
              헌금 · 헌금
```

- **요약이 내놓는 수는 둘뿐이다.** 셋이 되는 순간 어느 것을 봐야 하는지 흐려진다.
  월 수지는 두 수의 차라 눈으로 읽을 수 있고, 통장 잔액은 `⋯` 메뉴의 통장 화면에
  있다. 두 수는 **이 달에 오간 돈**이고 잔액은 **쌓인 돈**이라, 나란히 세우면
  같은 종류의 수로 읽힌다.
- **헤더는 인스타그램 프로필 문법이다 — 왼쪽은 만들기, 오른쪽은 관리다.**
  왼쪽에 매달 스무 번 넘게 누르는 **거래 추가(＋)**, 오른쪽에 가끔 쓰는 것을 모은다:
  **분류 붙이기(태그 아이콘)** 는 제 버튼으로 서고, 그 오른쪽 **햄버거(≡)** 가
  풀스크린 메뉴를 연다. 통장 잔액은 늘 보는 수가 아니라 가끔 확인하는 수라 메뉴 한
  단계 안이 알맞고, 분류 붙이기는 장부를 쓰는 본업이라 메뉴 밖으로 냈다.
- **햄버거(≡)는 드롭다운이 아니라 풀스크린 메뉴(`FinanceMenuView`)로 빠진다** —
  인스타그램 프로필의 ≡ 문법이다. 항목마다 아이콘·설명이 붙어 드롭다운보다 읽기
  쉽고(통장 잔액 · 보고서 미리보기 · 월별 보고서 PDF · 영수증 부록 PDF), 앞으로
  늘어날 자리도 넉넉하다. 행은 프로필 탭의 `actionRow` 와 같은 카드 문법이다.
  풀스크린 위에는 시트를 바로 얹지 못해, 메뉴는 갈래만 넘기고 **닫힌 뒤**
  부모가 실행한다(`FinanceView.runPendingMenuAction`).
- **재정 탭에는 알림 종이 없다.** 헤더가 어느 탭에서나 직접 그리던 것인데, 알림은
  "청구가 들어왔다" 는 소식이라 청구서 탭의 것이다. 그 자리는 거래 추가·관리 동작이
  쓴다 — 농협은 수기 입력이 유일한 입구라 거래 추가는 메뉴 두 단계 안에 둘 자리가
  아니었다. `AdminHeaderView(showsNotifications:)` 로 끈다.
  **자리를 지키자고 안 쓰는 버튼을 두지 않는다.**
- **라벨이 위, 숫자가 아래다.** 여기서는 "무엇의 수인지"를 먼저 알아야 수가 읽힌다.
  큰 수 하나가 홀로 서는 자리(시트 머리 금액)와는 다르다.
- **날짜 셀렉터는 주 단위로 끊긴다.** 자유롭게 흐르게 두면 요일 자리가 매번 달라져서,
  늘 같은 칸에 있어야 할 "토요일"을 눈으로 못 찾는다. 달 경계에 걸친 주는 앞뒤 달
  날짜까지 일곱 칸을 채우되 **그 칸은 흐리고 눌리지 않는다.**
- 날짜를 **한 번 더 누르면 풀린다.** 되돌아갈 길을 따로 만들지 않으려고 같은 자리를
  토글로 쓴다.
- **정렬 줄은 목록 바로 위, 오른쪽 끝이다.** 갈래가 최신순·오래된 순 둘뿐이라
  세그먼트로 폭을 나누지 않고 `Menu` 로 접는다 — 고른 갈래가 라벨에 그대로 적혀
  펼치지 않아도 읽힌다. **목록이 있을 때만 뜬다**(빈 범위 위에 홀로 서면 무엇을
  정렬하는 건지 알 수 없다). 정렬은 뷰모델의 `oldestFirst` 한 값이 갖고, 네 칩이
  모두 `ledgerRows(of:)` 를 지나가 함께 따라간다.
- **목록의 한 줄은 거래가 아니라 장부 줄이다.** 묶어 보낸 출금 하나는 조각마다
  한 줄이 된다 — 사람이 쓰던 엑셀이 그 모양이고, 보고서·분석도 이미 그 단위로 센다.
  목록만 거래 단위이던 시절에는 한 앱 안에 단위가 둘이었다.
  통장과 한 줄씩 대조하는 일은 **거래내역서 확인 화면**이 맡는다.
- **칩 개수도 장부 줄 수다.** 엑셀 장부의 줄 수와 같은 수라야 한다.
- **미분류가 넷째 칸이다.** 분류를 붙이는 건 "남은 것" 을 보는 일이라 그 수가 보여야
  한다. 내부 이체는 안 센다 — 앞으로도 분류가 안 붙는 줄이라 세면 영영 0이 안 된다.
- **분류는 넣을 때가 아니라 목록에서 붙인다.** 거래 추가 화면에는 분류 칸이 없다.
  스무 번 넘게 반복하는 자리에서 매번 고르는 건 무겁고, 그 자리에서는 무엇으로
  묶을지 정하기도 어렵다. **목록에서는 비슷한 줄이 나란히 보여서 이름이 저절로
  정해진다** — 헤더의 **분류 붙이기(태그 아이콘)** 로 여러 줄을 골라 한 번에 준다. 청구서 탭의
  선택 모드와 같은 문법이고, 이 앱에서 목록 줄을 "눌러서 고르는 것" 으로 바꾸는
  두 번째 자리다.
- **거래 행은 금액이 제목 자리다.** 왼쪽 첫 줄에 오고, 무엇에 쓴 돈인지가 그 아래
  회색 부제다. 장부를 훑을 때 눈이 먼저 잡는 건 결국 수다.
- 행이 내놓는 것은 **둘뿐이다** — 금액과 부제 한 줄. 시각·잔액·메모는 상세로 내렸다.
- **그래프는 두 선이 같은 자를 쓴다.** 각자 정규화하면 더 쓴 달이 오히려 낮아 보이는
  거짓말이 된다. 가로축도 긴 달 기준이라야 같은 날짜가 같은 자리에 놓인다.

### 분석 화면 (`SpendingDetailView`)

"자세히 보기"가 여는 시트다. 밴드가 한 줄로 흘려 말한 것을 **크게 다시 말하고,
왜 그런지까지 붙인다.**

```
닫기 ✕                        ← 오른쪽 위. 상세 시트와 같은 자리
2026년 8월                     ← 어느 달인지. 여기서는 달을 넘기지 않는다
지난달보다 62만원 더 나갔어요      ← 밴드와 같은 문장을 키운 것
                   ● 8월 ● 7월  ← 두 선이 무엇인지
[두 선 그래프]   8.1 ── 8.31
────────────────
소비 분석
[출금 14] [입금 5]
헌금에 가장 많이 나갔어요
[비중 막대]
· 헌금 320,000원 · … · 그 외 8개
```

- **문장은 한 곳에서 나온다** (`SpendingComparison`). 밴드와 이 화면이 문장을 각자
  만들면 같은 달을 두고 다른 말을 하게 된다. 화면이 정하는 건 크기뿐이다.
- **그래프도 한 벌이다** (`SpendingSparkline` 의 `inline` ↔ `expanded`). 작은 그래프를
  눌러 큰 그래프가 열리는 구조라, 둘이 다르게 그려지면 같은 것이라는 게 안 읽힌다.
  큰 쪽만 지난달 선 아래를 옅게 채우고 가로 끝에 날짜를 붙인다 — 작은 자리에서는
  면이 선을 삼켜서 두 선의 벌어짐이 안 보인다.
- **날짜 축도 거래 목록도 여기 없다.** 본화면이 이미 갖고 있다. 목록으로 돌아가는
  길은 닫기다. 달을 넘기는 자리도 없다 — 어느 한 달을 들고 들어오는 화면이라
  여기서 달이 바뀌면 뒤에 남겨 둔 목록과 어긋난다.
- **입금도 나눠 본다.** 레퍼런스는 개인 소비라 지출만 보지만, 교회 장부는 헌금이
  지출만큼 중요하다.
- **카테고리 색은 한 색의 명도 사다리다** (`Palette.categorySeries`). 이 앱은 색마다
  뜻이 붙어 있어서(파랑 입금 · 주황 주의 · 초록 완료) 카테고리에 그 색들을 빌려
  주면 뜻이 흐려진다. 읽어야 하는 건 "어느 것이 제일 큰가" 지 색 이름이 아니다.
- 이름을 적는 건 **셋까지**고 넷째부터는 "그 외 N개" 한 줄이다. 카테고리가 열 개인
  달에 열 줄을 세우면 1등이 안 보인다. 사다리를 더 늘려도 아래 두 단이 구별되지 않는다.
- 카테고리는 **`reportItems` 로 센다.** 분할 거래는 조각마다 카테고리가 다르므로
  거래 단위로 세면 통째로 첫 조각의 카테고리에 들어간다. 묶는 이름(`categoryLabel`)은
  보고서와 같은 규칙이라야 두 곳의 합계가 어긋나지 않는다.

---

### 거래내역서 확인 화면 (`StatementImportView`)

```
헤더        닫기      거래내역서 확인
섹션 목록    확인이 필요해요 2
              09-02  이충성        -104,900원  ›
            청구서와 맞았어요 3
              08-20  이충성        -946,501원  ›
                · 아침식사        414,000원
                · 저녁식사 재료    236,310원
            청구가 없어요 4
            눌러서 적요와 분류를 적어 두면 넣을 때 같이 들어가요.
              08-20  ATM현금        -30,000원  ›
                · 8월 간식비        30,000원   ← 적으면 여기 미리 보인다
            통장 사이 이체예요 1
              08-20  예수교대한성결고천교  4,000,000원  ›
                농협 ↔ 모임 · 합계에서 빠져요
            이미 장부에 있어요 14  ⌄        ← 접혀 있다
────────────────
바닥 바      2건은 어느 청구인지 아직 안 정했어요
            9건 넣어요              -1,234,567원
            [ 장부에 넣기 ]
```

- **일이 남은 것이 맨 위다.** 내역서 순서대로 세우면 손댈 줄을 찾으려고 스무 줄을
  훑어야 한다.
- **건수와 합계는 바닥 바에 있다.** 이 화면이 답하는 질문이 "몇 건이 얼마나
  들어가나" 라서 결정과 같은 자리에 둔다 — 묶어 보내기의 `selectionBar` 와 같은
  문법이다. 위에 요약을 또 두면 같은 수를 두 번 적게 된다.
- **결정을 오른쪽 위에 두지 않는다.** 화면의 유일한 행동이 가장 안 눌리는 자리에
  앉아 있던 적이 있다. 지금은 바닥의 `ActionButton(.primary)` 다.
- **아직 안 들어간 줄은 전부 누를 수 있다.** 예전에는 청구 후보가 있는 줄만 눌렸는데,
  이제 어느 줄이든 적요와 분류를 적을 수 있다. 화살표가 없는 건 이미 장부에 있는
  줄뿐이다.
- **줄을 누르면 장부에 뭐라고 적힐지가 그 자리에서 보인다.** 적요를 치는 동안 아래
  "장부에 이렇게 적혀요" 가 같이 바뀐다. 넣고 나서야 결과를 아는 화면이 아니다.
- **청구를 고른 줄에는 적요 칸을 안 준다.** 적요가 청구 제목에서 오는데 적는 자리까지
  있으면 어느 것이 맞는지가 흔들린다. 분류는 청구에 없는 값이라 늘 열려 있다.
- **"이미 장부에 있어요" 는 접혀 있다.** 같은 달을 다시 뽑으면 그 섹션이 제일
  길어서, 펼쳐 두면 정작 손댈 줄이 화면 밖으로 밀린다.
- **설명은 줄을 봐도 모르는 것에만 준다.** 네 섹션 전부 두 줄짜리 설명이 붙어서
  설명이 항목보다 많던 적이 있다. 후보가 몇 개인지·장부에 뭐라고 적힐지는 줄
  자체가 말한다.
- **안 정한 게 있어도 넣기를 막지 않는다.** 안 정한 채로 넣으면 청구 없이 들어갈
  뿐이고 나중에 고칠 수 있다. 잠그면 빠져나갈 길이 없어진다. 대신 바닥 바가
  주의색으로 한 줄 말한다.

### 통장 화면 (`AccountBalanceView`)

헤더 왼쪽 통장 버튼이 연다. **잔액을 저장하지 않기 때문에 있는 화면**이다 —
`finance_transactions.balance` 를 없앤 뒤로 "지금 얼마 있나" 를 말하는 자리가
앱 어디에도 없었다.

```
닫기 ✕
9,128,884원                   ← 총 재정. 큰 수 하나가 홀로 선다
지금 통장에 있는 돈을 모두 합한 금액이에요
────────────────
농협                5,783,084원
모임                3,345,800원
────────────────
통장 사이에서 오간 돈
농협 → 모임          4,000,000원
장부 전체로 보면 나간 돈도 들어온 돈도 아니라, 총 입금·총 출금과 보고서에는 들어가지 않아요
```

- **수가 먼저고 문장이 아래다.** 요약 밴드의 두 칸(라벨 위·수 아래)과 반대 순서인데,
  큰 수 하나가 홀로 서는 자리라서다. 상세 시트 머리와 같은 문법이다.
- **보고 있는 달의 끝을 기준으로 센다.** 이번 달이면 아직 오지 않은 끝이라 곧
  지금 잔액이고, 지난달을 펼쳐 두고 열면 그 달이 끝났을 때의 잔액이다.
  여기만 오늘을 말하면 두 화면이 다른 시점을 말하게 된다.
  이 성질이 **검산**이기도 하다 — 모임통장은 월말에 잔액을 농협으로 돌려보내므로
  지난달 끝에서 0 이어야 한다.
- **통장 한 줄에 수 하나다.** 개시잔액도 그 달 입출금도 적지 않는다. 이 화면이
  답하는 질문은 "지금 얼마 있나" 하나다.
- **마이너스 잔액만 위험색이다.** 출금이 검정인 것과 다르다 — 출금은 일상이지만
  통장이 마이너스인 건 어긋난 것이다.
- **"통장 사이에서 오간 돈" 은 어긋나 보이는 두 수를 설명하는 자리다.** 예산
  400만원을 옮긴 달은 통장 잔액이 크게 움직이는데 요약 밴드의 총 출금은 꿈쩍도
  안 한다(합계에서 빼기 때문에). 설명이 없으면 둘 중 하나가 틀린 것처럼 보인다.
- 달을 넘기는 자리가 없다. 분석 화면과 같은 이유다.

---

## 이 규칙 밖에 있는 것

`FinanceReportExporter` 는 화면이 아니라 **출력물**이라 `UIFont` 로 직접 그린다.
Dynamic Type 도 안 타고 종이 위에서만 읽히므로 위 규칙을 적용하지 않았다.
손대려면 보고서 전체를 같이 봐야 한다.

**보고서 미리보기(`FinanceReportPreviewView`)도 여기 딸려 들어온다.** 표를 다시
그리지 않고 **PDF와 같은 HTML** 을 `WKWebView` 로 띄운다. 집계 로직이 한 벌이라
화면과 출력물이 어긋날 수 없는 대신 **`DS` 도 Dynamic Type 도 안 탄다** — 색과
글자 크기가 `screenCSS` 안의 CSS에 있다.

`forScreen: false` 일 때 `screenCSS`·`twOpen`·`twClose` 가 전부 빈 문자열이라
**PDF 로 가는 HTML 은 한 글자도 다르지 않다.** 화면용 CSS를 손볼 때 이 성질을
깨뜨리지 말 것 — 깨지면 종이 출력이 조용히 같이 바뀐다.

---

## 아직 안 본 것

1. **다크 모드.** 어두운 쪽 값은 **이 앱이 정한 것**이다 — 원문은 밝은 모드만
   발행하고, 다크 alias 표가 없다는 걸 원문 스스로 Known Gaps 첫 줄에 적어 뒀다.
   무채색 사다리를 뒤집고 유채색은 한 단 올린 명도를 쓴다.

   **2026-09-03 에 실기기로 처음 봤다.** 걸린 건 하나 — 선택된 칩이 흰 바탕에 흰
   글자였다. `Ink.inverse` 하나가 무채색 채움과 유채색 채움을 겸하고 있어서였고,
   `Ink.onAccent` 를 갈라 내 고쳤다 ([TROUBLESHOOTING.md](TROUBLESHOOTING.md) 맨 위).
   **고친 뒤 같은 날 실기기에서 다시 봤고 칩도 파랑 버튼도 제대로 읽힌다.**
   나머지 화면은 그대로 읽혔다. 아직 못 본 자리는 **빈 상태가 아닌 청구서 목록의
   상태 배지**와 **보고서 미리보기**다 — 배지 색 쌍은 다크 값이 따로 있고, 미리보기는
   `DS` 를 안 타고 `screenCSS` 안의 CSS 로 그려서 이 시스템 밖이다.
2. **큰 글씨(Dynamic Type).** 글자 크기가 시맨틱 폰트에서 고정 pt 사다리로 바뀌면서
   확대 폭이 달라졌다. 특히 **날짜 셀렉터 칸**(일곱 칸이 화면 폭을 나눠 가짐)과
   **요약 밴드 두 칸**이 버티는지 봐야 한다.
3. **탭바.** iOS 26 기본 `TabView` 가 그리는 Liquid Glass 라 이 시스템 밖에 있다.
   직접 그리려면 `ContentView` 를 손봐야 한다.
4. **주간 스트립의 앞뒤 달.** 달 경계 주에서 이전·다음 달 날짜를 흐리게만 두는데,
   그 칸을 누르면 달을 넘기는 게 나을 수도 있다.
