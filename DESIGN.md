# 디자인 가이드라인

이 문서는 **이미 앱에 있는 규칙을 적은 것**이지 새로 정한 취향이 아니다.
값은 전부 `NanuriAdmin/Components/DesignSystem.swift` 의 `DS` 에 이름으로 들어 있다.
**화면 코드에 숫자를 직접 적지 않는다.** 필요한 값이 없으면 먼저 `DS` 에 이름을 붙인다.

관리자 1인이 쓰는 앱이고, 화면마다 정보 밀도가 높다. 그래서 아래 규칙들은
"예뻐 보이려고"가 아니라 **한 화면에 많은 정보를 넣고도 읽히게** 하려고 있는 것이다.

---

## 1. 화면의 뼈대

모든 탭은 같은 구조다.

```
화면 배경 systemGroupedBackground  (안전영역까지)
├─ 헤더 AdminHeaderView   배경 없음 — 화면 배경 위에 그냥 얹힌다
└─ 카드  systemBackground · radius 16 · 그림자 6%
```

**헤더에 배경색도 구분선도 주지 않는다.** 목록이 헤더 아래로 지나가지 않으므로
(같은 `VStack` 안이다) 경계를 그릴 이유가 없다. 상단에 색이 다른 띠가 하나 더
생기면 화면이 그만큼 낮아 보인다. 그래서 `screenBackground()` 는 안전영역까지
덮는다 — 안 그러면 상태바 자리에만 다른 색이 남는다.

### 헤더 (`AdminHeaderView`)

**모든 탭이 이 헤더를 쓴다.** 내비게이션 바는 전부 걷어냈다 — `NavigationView` +
`.navigationTitle` 을 섞어 쓰던 시절엔 탭마다 상단 높이도 제목 크기도 달랐다.

슬롯은 넷이고 자리가 고정돼 있다.

| 자리 | 내용 |
| --- | --- |
| 왼쪽 | 화면별 동작 하나 (`leading:`). 없으면 빈다 |
| 가운데 | **탭 이름** |
| 오른쪽 | 화면별 동작 하나 (`trailing:`) |
| 맨 오른쪽 | 알림 (헤더가 직접 갖는다) |

- 알림만 어느 탭에서나 같은 자리에 있어야 해서 **화면이 넘기지 않는다.**
- **새로고침 버튼은 없다. 목록을 아래로 당겨서 새로고침한다** (`.refreshable`).
  청구서가 실시간으로 들어오게 되면서 버튼을 누를 일이 거의 없어졌고, 당기기는
  어느 목록에서나 되는 손동작이라 자리를 차지하지 않는다. 비워진 왼쪽은 화면이
  쓴다 — 청구서 탭의 묶어 보내기 선택 모드가 거기 있다.
- **빈 화면에서도 당길 수 있어야 한다.** `.refreshable` 은 스크롤되는 것에만
  붙으므로 `EmptyStateView` 는 `.pullToRefresh { }` 로 감싼다. 버튼을 걷어낸 이상
  목록이 비었을 때 새로고침할 길이 없으면 막힌다.
- 화면마다 다른 동작은 **한쪽에 하나씩만** 둔다. 둘 이상이면 `Menu` 하나로 묶는다
  — 재정 탭의 `ellipsis.circle` 이 그 예다. 아이콘을 늘리면 가운데 이름이 밀린다.
- 가운데를 눌러야 하는 화면은 `titleAction` 을 준다. 그러면 이름 옆에
  `chevron.down` 이 붙는다 (재정 탭 → 장부 게이트로 되돌아가기).
  **장부 이름처럼 "지금 무엇을 보는 중인지"는 제목이 아니라 화면 안에 적는다**
  (재정 탭은 요약 카드 첫 줄).
- 안 읽은 알림은 **주황 점** 하나로만 알린다. 빨강이 아닌 이유는 5번에 있다.

내비게이션 바가 없으므로 `.searchable` 도 못 쓴다. 검색은 헤더 바로 아래
`SearchField` 를 놓는다 (계좌부 탭).

- 목록은 `List` + `.listStyle(.plain)` + `.screenBackground()`
- 행은 `BillRowView` 처럼 **카드 하나**다. `.cardStyle()` + `.cardRow()`
- `Form` 은 **입력 화면에서만** 쓴다 (`PayeeEditView`, `TransactionEditView`).
  목록에는 쓰지 않는다 — 카드 구조와 섞이면 두 가지 배경이 한 화면에 나온다.
- 시트 안에서 라벨-값 줄을 묶을 때는 `.groupBox()` 다. **카드와 배경색이 반대**인데
  (안쪽이 `systemGroupedBackground`) 시트 바닥이 이미 `systemBackground` 라서
  같은 색으로 칠하면 상자가 안 보이기 때문이다.

```swift
List(bills) { bill in
    BillRowView(bill: bill)      // 내부에서 .cardStyle()
        .cardRow()               // 목록 행일 때의 여백 + List 장식 제거
}
.listStyle(.plain)
.screenBackground()
```

`.cardRow()` 가 구분선을 지우는 건 취향이 아니다. 카드가 자기 그림자를 갖고 있어서
구분선이 같이 그려지면 카드 밑에 선이 겹쳐 보인다.

### 카드는 정보, 결정은 시트

**목록 카드에 액션 버튼을 넣지 않는다.** 카드는 읽는 것이고, 카드를 누르면 열리는
시트가 그 항목으로 할 수 있는 일을 전부 갖는다. 청구서(`BillRowView` →
`BillDetailView`)와 계좌부(`PayeeListView` → `PayeeEditView`)가 같은 구조다.

청구서 카드에는 원래 36×36 버튼이 네 개(영수증·송금·거절·삭제) 금액 옆에 붙어
있었다. 두 가지가 잘못됐다 — **송금과 거절이 6pt 간격으로 나란히 있었고**,
글씨를 키우면 금액과 버튼 네 개가 한 `HStack` 이라 그 줄부터 무너졌다.
목록에서 한 번에 되는 편의보다 잘못 누르지 않는 게 우선이다.

카드 한 장이 담는 건 **두 줄, 다섯 가지**다. 더 늘리지 않는다.

```
[이니셜 원 44]  이름                     금액
               항목                   [상태 칩]      >
```

날짜·계좌 같은 나머지는 전부 시트로 내린다. 목록에서 훑을 때 필요한 건 "누가
얼마를 무엇으로 청구했고 처리됐는가" 까지다. 카드에 한 줄을 더 붙이고 싶어지면
그건 대개 시트에 있어야 할 것이다.

**예외는 선택 모드 하나뿐이다.** 청구서 탭 헤더 왼쪽의 `checklist` 를 누르면
카드가 "눌러서 고르는 것"이 된다 (같은 사람 여러 건을 묶어 한 번에 송금). 이때는
이니셜 원 자리에 체크가 들어오고 오른쪽 화살표가 빠진다 — 눌러도 시트가 안 열리는데
화살표를 두면 거짓말이 된다. 못 고르는 카드(다른 사람 · 대기중 아님 · 계좌 미등록)는
40% 로 흐려 둔다. 카드에 상시 버튼을 붙이지 않는다는 규칙은 그대로다. 선택은
**모드에 들어가야만** 생기고, 나올 때 사라진다.

이니셜 원(`InitialAvatarView`)은 줄을 잡아 주는 표식일 뿐이라 **사람마다 색을
다르게 주지 않는다.** 색은 뜻이다 (5번).

### 시트 높이는 화면 비율로 고정한다

`.medium`(화면 절반)은 내용과 아무 관계가 없어서 하필 "영수증 보기" 버튼 자리를
잘랐다. 잘린 글은 "아래에 더 있다"는 신호지만 **잘린 버튼은 눌러도 되는 건지부터
헷갈린다.**

그래서 한동안 **내용을 재서 딱 맞췄는데, 열 때마다 높이가 달라졌다.** 같은
청구서가 어떨 때는 길게 어떨 때는 짧게 열렸다 (`TROUBLESHOOTING.md`). 지금은
`.fraction(DS.Sheet.billDetail)` — **화면 높이의 정해진 비율**이다. 늘 같은 자리에서
열리는 게, 매번 딱 맞지만 매번 다른 것보다 낫다. `.large` 는 손으로 더 올릴 때를
위해 남긴다.

비율은 내용보다 **조금 넉넉하게** 잡는다. 남으면 스크롤 안쪽 아래에 빈 자리가
생길 뿐이지만, 모자라면 버튼이 잘린다. 값과 계산 근거는 `DS.Sheet.billDetail` 에
적어 뒀다.

**스크롤 안 마지막 버튼과 바닥 바 사이는 `DS.Spacing.sheetEdge`(32)만큼 띄운다.**
붙어 있으면 "영수증 보기"를 누르려다 그 아래 송금·거절을 짚는다.

### 같은 값을 한 시트에 두 번 적지 않는다

`BillDetailView` 머리에는 원래 이니셜 원 + 이름 + 계좌 + 상태 칩이 있었고, 바로
아래 상자에 상태와 계좌가 또 있었다. **한 화면에 같은 값이 두 번 있으면 어느 쪽을
봐야 하는지 읽는 사람이 판단해야 한다.** 이름까지 상자의 한 줄로 내려서 값이 적히는
곳을 상자 하나로 모았다. 시트 머리에 남는 건 금액뿐이다.

계좌가 없을 때도 줄을 지우지 않는다 — 없다는 사실이 거기서 할 결정(송금 대신 계좌
등록)을 바꾸므로 "계좌 미등록"을 주황으로 적는다.

## 2. 여백

| 이름 | 값 | 쓰는 곳 |
| --- | --- | --- |
| `DS.Spacing.screen` | 16 | 화면 좌우 여백, 카드 안쪽 여백 |
| `DS.Spacing.cardGap` | 6 | 세로로 이어지는 카드 사이(위아래 각각) |
| `tight` / `small` / `medium` / `section` | 4 / 8 / 12 / 20 | 요소 사이 |
| `DS.Spacing.sheetEdge` | 32 | 시트 안쪽 위아래 — 손잡이와 금액 사이 · 마지막 버튼과 바닥 바 사이 |

시트 안쪽만 32 인 이유는 두 가지다. 위는 앱에서 가장 큰 글자(금액 34)가 손잡이에
붙지 않게, 아래는 **스크롤 안 버튼과 바닥에 고정된 버튼이 붙지 않게** — 20 으로는
둘이 한 덩어리로 보여서 누를 때 잘못 짚는다.

화면 좌우 여백과 카드 안쪽 여백이 **둘 다 16** 인 게 중요하다. 이게 어긋나면
카드 안 글자와 화면 제목의 세로선이 안 맞는다.

글이 긴 카드는 `.cardStyle(padding:)` 으로 안쪽 여백만 키운다. 카드 모양·그림자는
그대로 둔다.

## 3. 글자

**크기는 다섯 단계(34 · 20 · 17 · 15 · 12), 굵기는 세 단계뿐이다.**
아래 표 밖의 조합은 쓰지 않는다.
(pt 는 Dynamic Type 기본(Large) 기준이고 사용자가 키우면 같이 커진다)

| 역할 | 폰트 | 굵기 | 모디파이어 |
| --- | --- | --- | --- |
| 상세 시트의 금액(숫자) | `.largeTitle` 34 | semibold | `.heroAmount()` |
| 카드 제목 · 강조 금액 | `.title3` 20 | semibold | `.cardTitle()` |
| 빈 상태 제목 | `.title3` 20 | semibold | `EmptyStateView` |
| 화면 제목 | `.title3` 20 | regular | `.headerTitle()` |
| 금액 뒤의 "원" | `.title3` 20 | regular (회색) | `.amountUnit()` |
| 시트 상자의 값 | `.headline` 17 | regular | `.sheetTitle()` |
| 행 제목 · 이름 · 금액 · 값 | `.subheadline` 15 | semibold | `.rowTitle()` |
| 시트의 설명 | `.subheadline` 15 | regular | `.sheetSubtext()` |
| 본문 · 설명 · 날짜 · 계좌 줄 | `.caption` 12 | regular | `.rowSubtext()` |
| 칩 · 상태 배지 | `.caption` 12 | regular | `.tagChip(color:)` |

### 시트는 목록보다 한 계층 크다

목록은 여러 건을 촘촘히 쌓는 자리고, **시트는 한 건만 들여다보는 자리다.**
목록의 15/12 를 시트에 그대로 가져오면 화면은 넓은데 글자만 작아 보인다.
그래서 시트 안에서는 제목·값이 17(`.sheetTitle()`), 설명이 15(`.sheetSubtext()`)로
한 계층씩 올라간다. **버튼은 안 올린다** — `ActionButton` 은 15 semibold 그대로다.
버튼은 읽는 글이 아니라 누르는 면이고, 높이(50)가 이미 크기를 말한다.

34 는 **상세 시트 머리의 금액 하나만** 쓴다. 한 화면에 하나뿐이라 크기를 독점시킬
수 있는 것이고, 목록 카드의 금액은 20(`cardTitle()`) 그대로다 — 거기선 여러 건이
나란히 놓이므로 하나만 커지면 줄이 안 맞는다.

### 화면 제목은 20 regular 다 (상단에 힘을 주지 않는다)

한동안 22 bold 였다. 헤더는 어느 탭에나 있고 아래 내용은 탭마다 다른데, 상단이
가장 굵고 크면 **매번 같은 글자가 화면에서 제일 세게 읽힌다.** 크기는 카드 제목과
같은 20 으로 내리고 굵기는 아예 뺐다 — 헤더는 양옆이 비어 있어서 그 정도로도
화면 이름으로 읽힌다. 그렇게 비운 맨 윗단은 금액(34)이 가져갔다.
로그인 화면 제목도 같은 모디파이어라 함께 내려간다.

### 숫자와 단위는 따로 논다

읽어야 하는 건 **숫자**다. "원"은 어느 청구서에서나 같은 글자라, 숫자와 같은 크기로
적으면 34pt 자리를 반쯤 나눠 갖는다. 그래서 `heroAmount()`(34 bold) 옆의 "원"은
`amountUnit()`(20 regular 회색)이고, 베이스라인을 맞춰 붙인다.
목록 카드의 금액은 그대로 한 덩어리다 — 20 짜리 한 줄에서 단위를 또 줄이면
그냥 작아 보이기만 한다.

### 굵기는 semibold · regular 둘뿐

- **semibold(600)** — 카드 제목 · 목록 행 제목 · 금액 · 버튼 글자 · 선택된 상태.
- **regular(400)** — 나머지. 설명 · 날짜 · 단위 · 비선택 상태, **화면 제목**,
  그리고 **시트 상자 안의 값**.

**bold(700)도 medium(500)도 쓰지 않는다.** bold 는 34pt 금액에서 눈에 띄게 무거워
그 줄만 화면에서 튀었고, medium 은 semibold 옆에 두면 차이가 눈에는 안 보이면서
"어느 걸 집을지"만 애매해졌다. 굵기가 둘이면 **고를 일이 없다** — 읽어야 하면
semibold, 아니면 regular 다.

굵기를 일부러 뺀 자리가 셋 있고 이유가 같다 — **이미 다른 것이 구분을 하고 있다.**

- **화면 제목**(`headerTitle()`)은 어느 탭에나 있다. 굵으면 매번 같은 글자가 제일
  세게 읽힌다. 자리(가운데)와 크기(20)가 이미 화면 이름이라고 말한다.
- **금액 뒤의 "원"**(`amountUnit()`)은 어느 청구서에서나 같은 글자다.
- **시트 상자 안의 값**(`sheetTitle()`)은 라벨(15 회색)과 크기·색으로 이미 갈린다.
  굵기까지 얹으면 값 네 줄이 한꺼번에 진해져서 **상자가 시트에서 제일 무거운
  덩어리**가 된다. 그 자리는 금액(34) 것이다.

`.headline` 은 시스템이 semibold 를 물고 오므로 `sheetTitle()` 에서 눌러 준다.

`.headline` 이 시스템에서 물고 오는 굵기가 semibold 라 이제는 팔레트 안의 값이다.
그래도 `sheetTitle()` 은 굵기를 **명시한다** — 코드에서 눈으로 확인할 수 있어야 한다.

### 12pt 아래로 내려가지 않는다

`.caption2`(11pt)는 쓰지 않는다. 칩·배지·개수처럼 작아도 되는 것들도 전부
`.caption`(12pt)이다. 한 화면에 정보가 많다고 글자를 더 줄이면 읽으라고 넣은
정보가 안 읽힌다.

위로도 마찬가지다. `.body`(17) · `.callout`(16) · `.footnote`(13)는 안 쓴다.
`.title`(28)과 `.title2`(22)도 이제 아무 데도 안 쓴다 — 맨 윗단은 `.largeTitle`(34)
하나이고, 그건 `heroAmount()` 한 곳만 쓴다.
`.subheadline`(15)·`.caption`(12)이 실질적인 본문 크기다 — 정보 밀도가 높아서
그렇다.

**텍스트에는 고정 pt 를 쓰지 않는다.** 시맨틱 폰트만 쓴다 (Dynamic Type 대응).
고정 pt(`.font(.system(size:))`)는 **SF Symbol 아이콘 전용**이고, 값은 `DS.Icon`
네 개뿐이다 (4번).

## 4. 아이콘

아이콘만 고정 pt 다. 네 단계뿐이다.

| `DS.Icon` | 값 | 쓰는 곳 |
| --- | --- | --- |
| `inline` | 15 | 카드 안 작은 액션 버튼(`BillRowView` 의 36×36 버튼) |
| `action` | 18 | 헤더 · 툴바 액션 |
| `feature` | 22 | 목록 행이나 카드의 대표 아이콘 |
| `placeholder` | 46 | 빈 화면 일러스트 |

## 5. 색

색은 **장식이 아니라 뜻**이다. 화면이 달라도 같은 뜻이면 같은 색이어야 한다.

| `DS.Palette` | 색 | 뜻 |
| --- | --- | --- |
| `deposit` | 파랑 | 입금 · 주요 동작(송금) · 첨부 |
| `withdrawal` | 빨강 | 출금 · 되돌릴 수 없는 동작(삭제 · 거절 · 로그아웃) |
| `pending` | 주황 | 대기 · 주의(계좌 미등록) |
| `done` | 초록 | 완료(송금됨) |

**빨강은 돈이 나가거나 무언가 사라질 때만 쓴다.** 강조하고 싶다는 이유로 쓰면
사용자가 위험 신호를 무시하기 시작한다.

이 넷 말고 다른 색을 화면에 들이지 않는다. 색을 늘리면 색이 뜻을 잃는다.

칩은 두 종류뿐이다 (`Components/ChipStyle.swift`).
- `.tagChip(color:)` — 읽기 전용 라벨. 배경은 그 색의 12%.
- `.selectableChip(isSelected:)` — 탭으로 토글.

배경 12%는 카드 위에서 글자가 읽히는 최소치다. 더 진하면 칩이 버튼처럼 보인다.

## 6. 컨트롤

- **목록을 거르는 컨트롤은 둘뿐이고, 개수로 고른다.** 어느 쪽이든 `Picker(.segmented)`
  는 쓰지 않는다 — 개수를 같이 보여줘야 하기 때문이다.
  - 둘·셋이면 `PillPicker`. 화면 폭을 균등하게 나눠 갖는다 (재정 탭의 전체·입금·출금).
  - 넷 이상이면 `ChipSelector`. 왼쪽부터 채우고 넘치면 가로로 스크롤한다
    (청구서 탭의 전체·대기중·완료·거절). 넷을 균등하게 자르면 칩 하나가
    "대기중 3" 을 못 담는다.
- **시트 바닥의 큰 버튼**은 `ActionButton`. 가로를 채우고 높이는
  `DS.Size.actionButton`(50), 글자는 15 semibold 다. 종류는 넷뿐이다 —
  `primary`(파랑, 하려던 일) · `secondary`(회색, 물러나기) · `tinted`(파랑 10%,
  결정은 아니지만 눈에 띄어야 하는 것) · `destructive`(빨강 10%, 되돌릴 수 없는 것).
  나란히 둘 때는 **되돌릴 수 없는 쪽을 왼쪽에** 놓는다. 오른손 엄지가 먼저 닿는
  자리가 오른쪽이다.
- **바닥에 버튼을 고정하면 그 위에 `Divider()` 를 넣는다.** 시트든 화면이든 같다
  (청구서 탭 선택 모드의 요약 바). 화면 바닥에 고정하는 바는 배경을
  `systemBackground` 로 깔아 목록과 층을 가른다. 헤더에 구분선을
  주지 않는 것과 어긋나 보이지만 이유가 정반대다 — 헤더는 목록이 그 아래로
  지나가지 않아서 그릴 경계가 없고, 고정된 버튼 위는 **내용이 실제로 거기서
  잘린다.** 선이 없으면 잘린 마지막 줄이 버튼에 붙어 한 덩어리로 보인다.
- **흰 시트 위에서 회색 면은 거의 안 보인다.** 버튼 배경은 `systemGray6` 이 아니라
  `systemGray5` 를 쓰고, `.groupBox()` 는 테두리 한 올(`separator`, 0.5pt)을 갖는다.
  화면(카드)에서는 그림자가 하던 일을 시트에서는 명도와 선이 한다 — 시트는 이미
  떠 있는 면이라 그 위에 그림자를 또 얹으면 층이 두 번 생긴다.
- **카드 안 액션 버튼**은 36×36, radius 10(`DS.Radius.button`), 아이콘 15pt.
  이제 목록 카드에는 쓰지 않는다 (1번).
- **헤더 버튼**은 `HeaderIconButton` (44×44, 아이콘 18pt, 배경 없음). 버튼이 아닌
  컨트롤(`Menu`)의 라벨은 `HeaderIcon` 을 쓴다 — 크기가 같아야 줄이 안 어긋난다.
  배경 캡슐은 없앴다. 아이콘 셋이 각자 떨어져 있으면 묶는 배경이 오히려 시끄럽다.
- **아이콘뿐인 버튼에는 `accessibilityLabel` 을 반드시 준다.** `HeaderIconButton`
  은 `label` 을 필수 인자로 받아서 빠뜨릴 수 없게 해 뒀다.
- 되돌릴 수 없는 동작(삭제·로그아웃)은 **확인을 한 번 받는다.** `alert` 또는
  `confirmationDialog`.

## 7. 움직임

두 가지만 쓴다.

| | 값 | 쓰는 곳 |
| --- | --- | --- |
| `DS.Motion.list` | spring(0.4, 0.8) | 목록 항목이 들고 날 때 |
| `DS.Motion.control` | spring(0.3, 0.7) | 컨트롤 선택이 바뀔 때 |

새 곡선을 만들지 않는다. 화면마다 미묘하게 다른 속도는 눈에 띄지 않으면서
앱을 조잡하게 만든다.

## 8. 빈 상태

`EmptyStateView` 를 쓴다. 아이콘은 선택이고, **무엇을 하면 채워지는지**를 적는다.

```swift
EmptyStateView(
    title: "등록된 계좌가 없어요",
    icon: "person.text.rectangle",
    message: "청구자 이름과 계좌를 미리 등록해 두면\n청구서가 들어올 때 자동으로 연결돼요."
)
```

## 9. 말투

- 화면의 모든 글은 **한국어 해요체**다. "저장되었습니다" 가 아니라 "저장했어요".
- 오류도 사람 말로 적는다. `error.localizedDescription` 을 그대로 화면에 올리는 건
  마지막 수단이다.
- 빈 상태·오류는 **다음에 뭘 하면 되는지**까지 적는다.

---

## 다음에 할 것

2026-08-19 기준. 위쪽 규칙은 **이미 코드에 반영돼 있고**, 아래는 아직 안 된 것들이다.
큰 것부터 적었다.

### 1. 다크 모드 점검 (한 번도 안 봤다)

카드가 `systemBackground`, 배경이 `systemGroupedBackground` 인데 **다크 모드에서는
이 둘의 명도 관계가 라이트와 반대**가 된다. 게다가 카드를 띄우는 수단이 6% 검정
그림자 하나뿐이라, 어두운 배경 위에서는 그림자가 사실상 안 보인다.
→ 다크에서 카드 경계가 흐려질 가능성이 높다. 실제로 켜 보고, 필요하면 다크에서만
아주 옅은 테두리를 주는 식으로 보완한다.

### 2. 큰 글씨(Dynamic Type)에서 카드가 버티는지

청구서 카드에서 버튼을 걷어내면서 제일 위험하던 줄(금액 + 버튼 4개)은 없어졌다.
남은 자리는 **카드 첫 줄(이름 + 금액)** 과 `BillDetailView` 바닥의 버튼 두 개다.
접근성 텍스트 크기로 한 번 훑어본다. 헤더 제목도 같이 본다.

시안에서 안 가져온 것 두 개도 여기 적어 둔다 — **상단 요약 카드**(대기 총액 +
건수)와 **거절 사유**. 요약 카드는 청구서 탭에 아직 없고, 거절 사유는 `bills` 에
컬럼이 없어서 지금은 왜 거절했는지 아무 데도 안 남는다.

### 3. 이번(2026-08-19 오후) 손본 것은 시뮬레이터까지만 봤다

시트 정보 중복 정리 · 금액 34 와 "원" 분리 · 굵기 두 단계(semibold · regular)는
시뮬레이터(iPhone 15, iOS 26.2)에서 확인했다. 시트 높이는 재서 맞추다 두 번
틀린 끝에 **화면 비율 고정**으로 정리했다 (`TROUBLESHOOTING.md`).

실기기에서 볼 것은 둘이다 — 비율(0.7)이 실제 청구서들에서 넉넉한지(항목 이름이
두 줄인 것으로), 그리고 **굵기를 두 단계로 줄인 뒤 목록 화면이 어떻게 읽히는지.**

### 4. 화면 확인 ✅ (2026-08-19 실기기)

헤더 · 글자 규칙 · 묶어 보내기 선택 모드 · 당겨서 새로고침 · 이미지 캐시까지
**실기기에서 동작을 확인했다.** 여기 있던 확인 목록은 다 지웠다.

다만 아래 1번(다크 모드)과 2번(큰 글씨)은 **평소 설정으로 쓰는 것과 다른 조건**이라
따로 켜 보기 전까지는 여전히 안 본 것으로 둔다.

### 5. 보고서는 이 규칙 밖에 있다

`FinanceReportExporter` 는 화면이 아니라 출력물이라 `UIFont` 로 직접 그린다
(369행에 `ofSize: 12, weight: .semibold`). Dynamic Type 도 안 타고 종이 위에서만
읽히므로 위 규칙을 적용하지 않았다. 손대려면 보고서 전체를 같이 봐야 한다.

**보고서 미리보기(`FinanceReportPreviewView`)도 여기에 딸려 들어온다.** 재정 탭
`⋯` 메뉴에서 열리는 이 화면은 표를 다시 그리지 않고 **PDF와 같은 HTML** 을
`WKWebView` 로 띄운다 (`makeReportHTML(forScreen:)`). 집계 로직이 한 벌이라
화면과 출력물이 어긋날 수 없는 대신, **DS 도 Dynamic Type 도 안 탄다** — 색과
글자 크기가 `screenCSS` 안의 CSS에 있다. 다크 모드는 거기 `prefers-color-scheme`
로 따로 대응한다.

`forScreen: false` 일 때 `screenCSS` · `twOpen` · `twClose` 가 전부 빈 문자열이라
**PDF 로 가는 HTML 은 이 작업 전과 한 글자도 다르지 않다.** 화면용 CSS를 손볼
때 이 성질을 깨뜨리지 말 것 — 깨지면 종이 출력이 조용히 같이 바뀐다.

아직 안 본 것: 이 화면은 **실기기·시뮬레이터에서 렌더링을 확인하지 않았다.**
표 폭(6열)이 세로 화면에서 어떻게 읽히는지, 다크 모드가 실제로 어떤지 봐야 한다.
