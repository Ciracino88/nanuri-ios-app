# 고쳐 본 것들

증상이 났을 때 **원인이 무엇이었고 왜 그렇게 고쳤는지**를 남긴다.
"이렇게 하세요"가 아니라 "이래서 이렇게 됐다"를 적는 문서다.
설정값은 `SETUP.md`, 화면 규칙은 `DESIGN.md`, 구조는 `CLAUDE.md` 에 있다.

새 항목은 **맨 위에** 붙인다.

---

## 2026-09-03 · 총 재정이 400만원 부풀었고, 앱 안에서 고칠 길이 없었다

**증상** — 통장 화면에서 농협 + 모임이 약 700만원으로 잡혔다. 모임은 맞는데
농협이 이상해 보였다.

**원인** — 두 겹이다.

**① 400만원 예산 수령 줄에 `counter_account_id` 가 없다.** 8월 모임 22건이
내부 이체 판정보다 **먼저** 들어가서, `ATM입금 4,000,000 예수교대한성결고천교`
가 평범한 입금으로 앉아 있었다. 모임은 실제로 받았으니 맞고, **농협에서 나간
사실이 어디에도 없어 농협이 400만원 많았다.** 총 재정도 그만큼 부풀었다.

거래내역서를 다시 불러와도 안 고쳐진다 — `alreadyImported` 로 걸러져서 새 판정이
그 줄에 닿지 않는다.

**② 편집 잠금이 너무 넓었다.** `saveTransactionEdits` 가 이랬다.

```swift
let finalCounter = locked ? existing?.counterAccountId : counterAccountId
```

거래내역서에서 온 거래는 상대 통장까지 잠갔고, 화면에서도 `transferRows` 가
`else` 가지에만 있어 **토글이 아예 안 보였다.** 그래서 ①을 앱 안에서 고칠 수 없었다.

**해결** — 잠금을 좁혔다. `counter_account_id` 는 `statement` 거래도 고칠 수 있다.

**금액·일시·통장은 은행이 말해 주는 사실이지만, "이게 통장 사이 이체인가" 는
은행이 말해 줄 수 없다.** 거래내역서에 그 정보가 없고 적요를 보고 사람이 내리는
회계 판단이다. 매처가 대신 해 주지만 못 잡을 수도 있고, **판정이 생기기 전에
들어온 줄은 아예 못 만난다.**

**재발 방지** — 잠금은 **"은행이 말해 준 값인가" 로 가른다.** 통장에서 읽어 온
값(금액·일시·통장)만 잠그고, 사람이 뜻을 붙이는 값(적요·분류·분할·이체 표시)은
연다. 새 칸을 잠글지 정할 때 이 기준을 쓴다.

그리고 **출력물만 보면 이런 어긋남이 안 드러난다.** 보고서는 `filteredExternal`
이라 내부 이체 줄을 원래 안 보여주므로, 400만원이 내부 이체로 잡혔든 평범한
입금으로 잡혔든 종이 위는 똑같다. **틀렸다고 말해 주는 건 통장별 잔액과 월말
0원 검산뿐이다.**

---

## 2026-09-03 · 공유로 넘긴 거래내역서가 앱에 안 들어왔다

**증상** — 파일 앱에서 PDF 를 공유 → 나누리를 고르면 **앱은 열리는데** 재정 탭으로
넘어가지도 않고 "저장된 거래내역서" 에도 안 생겼다. 오류도 안 떴다.

**원인** — `onOpenURL` 이 **두 군데** 있었고, 둘 다 어긋났다.

| 어디 | 언제 붙어 있나 | 하는 일 |
| --- | --- | --- |
| `NanuriAdminApp` 루트 | **항상** | `GIDSignIn.handle(url)` |
| `ContentView` 의 `TabView` | `authState == .loggedIn` 일 때만 | PDF 처리 |

두 가지가 겹쳤다.

1. **공유로 앱이 켜지는 순간엔 `ContentView` 가 계층에 없다.** `authState` 가
   `.loading` 이라 화면엔 `ProgressView` 만 있다. URL 은 실행 직후 도착하는데
   받을 핸들러가 안 붙어 있으니 그대로 사라진다. `checkSession()` 이 끝나 탭이
   떠도 URL 은 이미 지나간 뒤다.
2. **SwiftUI 는 `onOpenURL` 이 여럿이면 하나에만 준다.** 항상 붙어 있는 루트가
   받아 `GIDSignIn` 에 넘기고, Google 이 자기 것이 아니라며 무시하면 거기서 끝이다.

**보관 코드는 멀쩡했다** — `persistOriginalPDF` 까지 애초에 못 갔다. 그래서
오류도 안 떴다. 실패한 게 아니라 **불린 적이 없다.**

**해결** — 받는 곳을 하나로 모으고, 화면이 준비될 때까지 들고 있게 했다.

- `IncomingFile`(`App/IncomingFile.swift`)이 들어온 PDF 를 담아 둔다
- **URL 은 늘 붙어 있는 루트에서만 받는다.** PDF 면 `IncomingFile` 에 담고,
  아니면 `GIDSignIn` 으로 넘긴다
- `ContentView` 는 `onAppear`(켜지면서 들어온 경우)와 `onChange`(떠 있는 채로
  들어온 경우) 두 자리에서 꺼내 간다. 도착 시점이 둘이라 한 자리로는 안 된다

**재발 방지** — **`onOpenURL` 을 화면에 달지 않는다.** 조건부로 뜨는 화면에 달면
"그 화면이 떠 있을 때만 되는" 입구가 된다. 앱이 이미 떠 있을 때 시험하면 멀쩡히
동작해서 **찬 실행에서만 깨지는 것을 놓치기 쉽다.** 밖에서 들어오는 것은 늘
루트에서 받고, 화면은 받아 둔 것을 꺼내 간다.

---

## 2026-09-03 · 쪽 바닥 글자가 거래 적요에 붙었다

**증상** — 실제 발급본(2쪽, 23건)을 파싱하니 1쪽 마지막 거래의 적요가 이랬다.

```
08-20 11:55:22 | 출금 | -143580 | 적요 [박영훈단위: 원1 / 21 / 2]
```

`박영훈` 뒤에 쪽 바닥의 `단위: 원` 과 쪽 번호가 그대로 붙었다.

**원인** — 파서는 **날짜로 시작하지 않는 줄을 "앞 적요의 줄바꿈 연속" 으로 본다.**
토스 적요가 실제로 줄을 넘겨 이어지기 때문에 필요한 규칙이다. 대신 거기 걸리면
안 되는 것들을 `footerPrefixes` 로 걸러 왔는데 그 목록에 둘이 빠져 있었다.

- `단위: 원` — 쪽 바닥의 단위 표시
- `1 / 2` — 쪽 번호. **접두사로는 못 거른다.** 숫자로 시작하고 쪽마다 다르다.
  게다가 PDFKit 이 쪽마다 두 번씩 뱉는다 (그래서 `1 / 21 / 2` 로 두 번 붙었다)

**해결** — `단위` 를 목록에 넣고, 쪽 번호는 정규식(`^\d+\s*/\s*\d+$`)으로 따로
걸렀다. 둘 다 걸리면 `flush()` 해서 그 거래를 확정하고 버린다 — 표 머리글을
만났을 때와 같은 처리다.

**재발 방지** — **한 쪽짜리 거래내역서로 시험하면 이 버그는 절대 안 나온다.**
쪽 바닥이 없기 때문이다. 파서를 고칠 때는 **여러 쪽짜리 실물**로 확인한다.

그리고 확인은 눈으로 하지 않는다. **잔액 체인이 검산이다** — 파서가 읽은 금액을
과거에서 현재로 누적하면 문서에 적힌 `거래 후 잔액` 이 한 원까지 나와야 한다.
23/23 이 맞았고, 그래서 적요만 틀렸다는 걸 좁힐 수 있었다. 금액·날짜가 틀리면
이 체인이 먼저 깨진다.

---

## 2026-09-03 · 다크 모드에서 선택된 칩이 흰 바탕에 흰 글자로 사라졌다

**증상** — 어두운 화면에서 청구서 목록의 필터 칩 중 **선택된 하나만** 안 읽혔다.
바탕도 글자도 흰색이라 글자가 있는지조차 안 보였다. 안 눌린 칩들은 멀쩡했다.

**원인** — `DS.Ink.inverse` 가 **두 가지 서로 다른 바탕을 겸하고 있었다.**

| 쓰이던 자리 | 바탕 | 다크에서 필요한 글자색 |
| --- | --- | --- |
| 선택된 칩 · 날짜 셀렉터의 선택 칸 | `Ink.primary` (무채색) | **검정 쪽** |
| 주요 버튼 · 파괴적 버튼 · 로그인 버튼 | `Palette.accent`·`danger` (유채색) | 흰색 |

무채색 사다리는 모드를 따라 뒤집히고 유채색 채움은 안 뒤집힌다. 한 토큰이 둘을
겸하면 **어느 값을 넣어도 한쪽은 반드시 안 읽힌다.** 값이 `light: white, dark:
white` 였으니 뒤집히는 쪽이 깨졌고, 그게 칩이다 — 다크에서 `Ink.primary` 는
`grey50`(거의 흰색)이라 흰 바탕 위에 흰 글자가 됐다.

**해결** — 토큰을 둘로 갈랐다.

- `Ink.inverse` = `primary` 의 반대. **모드를 따라 같이 뒤집힌다** (`dark: grey900`).
  이름 그대로의 뜻으로 돌려놓은 것이다.
- `Ink.onAccent` = 유채색 채움 위의 글자. **두 모드 모두 흰색.**

바꾼 자리는 넷이다 — `ActionButton` 의 `.primary`·`.destructive`, `BottomCTA`,
`LoginView`, `FinanceHelpers` 의 "새 장부 만들기". 칩 셋(`ChipStyle`·
`ChipSelector`·`FinanceView` 의 날짜 칸)은 `inverse` 에 그대로 남는다.

**재발 방지** — **"뒤집힌 색" 을 하나로 두지 않는다.** 채움이 무채색이냐 유채색이냐로
토큰이 갈린다. 새로 채운 자리를 만들 때 바탕이 `Ink.primary` 면 `inverse`,
`Palette.*` 면 `onAccent` 다. 두 토큰의 주석에 각각의 이유가 적혀 있다.

밝은 모드만 보고는 절대 안 잡힌다 — 밝은 쪽에서는 두 값이 똑같이 흰색이라
어느 토큰을 써도 맞게 보인다. **[DESIGN.md](DESIGN.md) 의 "아직 안 본 것" 이
비어 있지 않으면 그런 종류의 버그가 남아 있다는 뜻이다.**

---

## 2026-09-03 · `supabase db push` 가 `SASL auth (SQLSTATE 28P01)` 로 튕겼다

**증상** — `supabase db push --linked` 가 풀러에 붙는 데까지는 가고 인증에서 죽었다.

```
failed SASL auth (FATAL: password authentication failed for user "postgres" (SQLSTATE 28P01))
```

**원인** — 비밀번호가 안 맞았다. `28P01` 은 `invalid_password` 라 네트워크·호스트·
링크 설정 문제가 아니다. **DB 비밀번호는 프로젝트를 만들 때 한 번 보여주고 다시는
안 보여준다** — 대시보드에 가도 값이 없고 `Reset database password` 버튼만 있다.
그리고 Supabase **계정 로그인 비밀번호와 다른 값**이라 헷갈리기 쉽다.

**해결** — 대시보드 `Project Settings > Database` 에서 **재설정**했다.
새 값으로 다시 돌리니 바로 적용됐다.

**재발 방지** — **재설정을 망설일 이유가 없다.** 이 프로젝트에서 DB 비밀번호를 쓰는
곳은 `supabase db push` 하나뿐이다.

| 쓰는 곳 | 값 |
| --- | --- |
| 워커 | `SUPABASE_SERVICE_ROLE_KEY` (다른 값) |
| 앱 | anon key (다른 값) |
| `db push` | **DB 비밀번호** |

그래서 재설정해도 앱도 워커도 안 깨진다. 기억이 안 나면 되찾으려 애쓰지 말고 바로
재설정한다. 비밀번호는 `SETUP.md` 1번대로 `read -rs` 로 받는다 — 명령줄에 직접
적으면 `~/.zsh_history` 에 그대로 남는다.

**대시보드 SQL Editor 로 우회할 수도 있지만 권하지 않는다.** 마이그레이션 SQL 을
직접 붙여 넣으면 비밀번호가 필요 없는 대신 **CLI 의 적용 이력에 안 남아**, 다음
`db push` 가 같은 마이그레이션을 또 적용하려다 실패한다. 굳이 그 길로 가야 하면
`supabase_migrations.schema_migrations` 에 `version` 을 같이 넣어야 한다.

---

## 2026-08-19 · 상세 시트가 "영수증 보기" 버튼을 반쯤 자른 채 열렸다

**증상** — 청구서 카드를 눌러 시트가 올라오면 "영수증 보기" 버튼이 아래쪽에서
잘린 채로 처음 화면에 걸렸다. 손으로 올리면 보이지만, 열자마자 보이는 건
"눌러도 되는지 모르겠는 반쪽 버튼"이었다.

**원인** — 시트 높이가 `.presentationDetents([.medium, .large])` 로 **고정**돼
있었다. `.medium` 은 화면 절반이고 내용과 아무 관계가 없다. 그 선이 하필 버튼
위를 지났다. 글이 잘리는 건 "아래에 더 있다"는 신호라 괜찮지만, 버튼은 다르다.

**해결** — 두 가지를 같이 했다.

1. **시트 높이를 화면 비율로 고정했다** — `.fraction(DS.Sheet.billDetail)`.
   내용보다 조금 넉넉하게 잡아서, 남으면 스크롤 안쪽 아래에 빈 자리가 생기고
   모자라면 스크롤된다. `.large` 는 손으로 더 올릴 때를 위해 남겨 둔다.
   (여기까지 오는 데 두 번 돌아갔다. 아래 "재서 맞추기" 참고.)
   또 **스크롤 안 마지막 버튼과 바닥 바 사이를 32(`sheetEdge`)로 띄웠다.**
   두 버튼이 붙어 있으면 "영수증 보기"를 누르려다 아래 송금·거절을 짚는다.
2. **내용을 줄였다.** 머리의 프로필 영역(이니셜 원 + 이름 + 계좌 + 상태 칩)이
   바로 아래 상자와 상태·계좌를 중복해서 갖고 있었다. 이름을 상자의 한 줄로
   내리고 머리를 통째로 걷어냈다. 시트가 짧아지고 값은 한 곳에만 남는다.

### 재서 맞추기를 두 번 시도하고 접었다

처음에는 `내용 + 바닥 바 + 안전영역` 을 더해서 한 번에 높이를 냈다. 안전영역은
`.ignoresSafeArea()` 를 준 배경에서 `safeAreaInsets.bottom` 을 읽는 흔한 요령으로
구했고, **시뮬레이터에서는 34 가 나와서 딱 맞았다.** 그런데 실기기(iPhone 15)에서는
그 값이 안 나와서 시트가 정확히 그만큼 짧았고, "영수증 보기" 버튼이 다시 잘렸다.
(시뮬레이터 프로브는 탭바 없이 띄웠고 앱은 `TabView` 안이라는 차이가 있었다.)

**그래서 안전영역을 아예 안 재기로 했다.** 대신 시트 높이 중 내용에 안 돌아간
몫을 **역산한다** — `요청한 높이 - 스크롤이 쓴 높이 - 바닥 바`. 처음엔 몫을 0 으로
보고 열고, 그때 드러난 몫으로 한 번 고쳐 잡으면 끝이다. `.height` 를 안전영역
포함으로 보든 아니든 맞는다 (시뮬레이터는 iPhone 15·17 Pro 둘 다 몫 0 이 나오고,
실기기에서는 34 가 나왔다).

여기서 세 번 미끄러졌다. 전부 **잰 값이 언제 것이냐**의 문제였다.

- **첫 계산의 출발점을 관측 높이로 두면 안 된다.** 아직 0 일 때 계산이 시작돼
  시트가 손바닥만 하게 열린다. 출발점은 **잰 내용 높이**여야 한다.
- **부족분을 관측 높이에 더하면 영원히 부족하다.** 관측값에서 이미 빠져 있는
  만큼이 매번 그대로 남아 같은 자리에서 멈춘다. 더하는 기준은 우리가 요청한
  높이여야 한다. (그래서 지금은 아예 절대값으로 목표를 다시 계산한다.)
- **크기가 바뀌는 도중의 값을 물면 안 된다.** 몫을 `max` 로 모으게 했더니 애니메이션
  중간값(59)을 물어서 시트 아래가 그만큼 비었다. 지금은 **시트가 우리 높이에 걸려
  있을 때만**(`selection` 으로 확인) 역산한다.

알아낸 몫은 기기마다 고정이므로 타입에 남겨 다음에 열 때 바로 쓰게 했다.

**그런데 실기기에서 같은 청구서가 열 때마다 다르게 열렸다.** 어떨 때는 내용보다
한참 길고 어떨 때는 살짝 잘렸다. 위의 세 함정을 다 막고도 남는 문제였다 — 결국
**첫 측정이 크기가 바뀌는 어느 순간에 걸리느냐**를 타고, 한 번 잘못 배운 값이
그다음 열기까지 따라간다. 재는 방식은 "맞을 때는 완벽하지만 언제 맞는지 보장할 수
없는" 쪽이었다.

**그래서 접고 비율로 고정했다.** 매번 딱 맞지만 매번 다른 것보다, 늘 같은 자리에서
열리는 게 낫다 (사용자 결정). 재는 코드(`onGeometryChange` 네 개와 상태 여섯 개)는
전부 지웠다 — 시트는 이제 `.fraction` 한 줄이다.

**재발 방지** — 시트 높이는 `DS.Sheet` 에 비율로 두고, 화면 코드에서 `.medium`
같은 뜻 없는 디텐트를 쓰지 않는다 (`DESIGN.md` 1번). 내용을 바꿔서 시트가 길어지면
**비율도 같이 손본다** — 근거(내용 몇 pt, 화면 몇 pt)는 `DS.Sheet.billDetail` 주석에
적어 둔다.

더 큰 교훈은 따로 있다. **레이아웃 중에 잰 값으로 레이아웃을 다시 정하는 구조는
"언제 재느냐"에 걸린다.** 한 번 맞는 걸 확인해도 늘 맞는다는 뜻이 아니다.
시뮬레이터에서 세 번 연속 같은 값이 나와도 실기기에서는 열 때마다 달랐다.

**곁가지** — 확인은 시뮬레이터에 띄운 **작은 프로브 앱**으로 했다. 이 앱은 로그인이
있어서 시뮬레이터에서 청구서 시트까지 못 간다. 시트 구조만 베낀 한 파일짜리 앱을
`swiftc` 로 빌드해 `simctl` 로 띄우면 잰 값(내용 378 · 바닥 82 · 안전영역 34)과
잰 값과 잘림 여부를 눈으로 볼 수 있다. **프로브는 앱과 같은 구조여야 하고**
(탭바 없이 띄웠다가 차이를 못 잡았다) **기기도 맞춰야 한다** — 필요하면
`simctl create ... iPhone-15 ...` 로 같은 기종을 만든다. 그래도 시뮬레이터와
실기기가 다르게 답하는 값이 있다는 게 이 항목의 결론이다. `xcode-select` 가 CommandLineTools 라
`simctl`·`swiftc` 앞에 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` 가
필요하다.

---

## 2026-08-19 · 영수증 사진이 매번 다시 받아지고 메모리를 크게 썼다 (→ Kingfisher)

**증상**
- 영수증 시트를 닫았다 다시 열면 사진을 처음부터 다시 받았다.
- 목록이 갱신되는 동안(Realtime) 사진이 아예 안 뜨는 일이 있었다.
- 썸네일 한 장에도 원본 해상도만큼의 메모리를 썼다.

**원인**
`AsyncImage` 는 세 가지를 안 해 준다.
1. 받은 이미지를 안 들고 있는다. 뷰가 사라지면 그걸로 끝이다.
2. 뷰가 다시 만들어지면 **로딩을 처음부터 다시 시작한다.** 부모가 자주 재렌더되는
   화면(실시간 목록)에서는 매번 리셋돼서 결국 아무것도 못 그린다. 아바타에서 먼저
   겪고 `CachedAvatarImage` 를 따로 만들었던 게 이 문제였다.
3. **원본 해상도로 디코드한다.** 폼이 올리는 영수증은 긴 변 1600px 라 파일은
   300KB 남짓이지만, 비트맵으로 펴면 1600×1200×4byte ≈ 7.7MB 다. 90pt 썸네일에
   그 전부를 편다.

**해결** — **Kingfisher** 로 옮겼다. 화면은 `Components/RemoteImage.swift` 의
`RemoteImage` 만 쓰고, 캐시(메모리·디스크)와 다운샘플링은 Kingfisher 가 한다.

- **다운샘플링** — `DownsamplingImageProcessor(size:)` + `.scaleFactor(displayScale)`.
  **이 둘이 한 세트다.** 크기는 pt 로 주고 배율을 따로 알려주는 구조라, `scaleFactor` 를
  빠뜨리면 3배 화면에서 뭉갠다. 그래서 호출부가 직접 `KFImage` 를 쓰지 않고 이 뷰를 거친다.
- `RemoteImage` 가 얇게 감싸는 이유가 하나 더 있다 — Kingfisher 의
  `onFailureImage(_:)` 는 `UIImage` 만 받아서 "아이콘 + 문구" 같은 실패 화면을 못 그린다.
  `.onFailure` 로 상태를 받아 View 로 그린다.
- 캐시 상한은 `RemoteImageCache.configure()` 에서 정하고 앱 시작 시 한 번 부른다.
  Kingfisher 기본값이 **디스크 무제한 · 7일 만료**인데, 영수증은 URL 이 곧 그 파일이라
  오래 들고 있어도 안전하다. 그래서 만료를 90일로 늘리고 대신 **용량(200MB)으로 끊는다.**
  만료가 짧으면 지난달 청구서를 다시 볼 때마다 새로 받는다.
- 손가락으로 확대하는 화면(`TransactionEditView` 의 미리보기)만 `maxDimension` 을 안 준다.
  줄여 놓으면 확대했을 때 뭉갠 게 그대로 보인다.

**중간에 한 번 돌아갔다.** 처음에는 의존성 없이 직접 만들었다(`NSCache` + 디스크 +
`CGImageSourceCreateThumbnailAtIndex`, 216줄). 동작은 같았지만 **유지보수**를 이유로
Kingfisher 로 갈아탔다 — 남이 읽을 때 설명이 필요 없고, 디스크 용량 상한·요청 취소·
프리페치처럼 우리가 안 만든 것들이 이미 들어 있다. 자체 로더는 지웠다. **캐시를 두
갈래로 두지 않는다** — 어느 쪽이 그 사진을 들고 있는지 모르게 된다.

**재발 방지** — 앱에서 `AsyncImage` 도 `KFImage` 도 직접 쓰지 않는다. 원격 이미지는 전부
`RemoteImage` 다. 확대 화면이 아니면 `maxDimension` 을 반드시 준다.

**`Cache-Control` 은 이제 앱과 무관하다** — R2 응답에 여전히 없지만(`SETUP.md` 8번),
Kingfisher 다운로더는 기본이 `URLSessionConfiguration.ephemeral` 이라 **URLCache 를 안 쓴다.**
헤더를 붙여도 앱이 요청을 아끼지 않는다. 자체 로더(`URLSession.shared`)를 쓰던 시절에는
의미가 있었는데, 캐시를 옮기면서 전제가 사라졌다.

### 곁가지: `Phase` 를 제네릭 뷰 안에 중첩하면 컴파일이 안 된다

직접 만들던 시절에 겪은 것이라 코드는 이제 없지만, 어떤 제네릭 뷰에도 해당한다.
`CachedAsyncImage<Content>` 안에 `enum Phase` 를 두고 클로저 인자로 넘겼더니
`generic parameter 'Content' could not be inferred` 가 났다. 클로저 본문의 타입으로
`Content` 를 추론해야 하는데 그 본문이 쓰는 `Phase` 가 `CachedAsyncImage<Content>.Phase`
라서 순환이 생긴다. `Phase` 를 타입 밖으로 빼면 풀린다. SwiftUI 의 `AsyncImagePhase`
가 `AsyncImage` 밖에 나와 있는 것도 같은 이유다.

---

## 2026-08-19 · 탭을 한 번 옮기면 실시간 반영이 죽었다

**증상** 청구서가 들어와도 목록이 그대로였다. 그런데 탭을 나갔다 들어오면 보였다.
그래서 "가끔 늦는다" 처럼 보이고 원인을 못 잡았다.

**원인** 구독을 `BillListView` 의 `.task` 안에서 통째로 돌리고 있었다.
1. 탭을 떠나면 화면이 사라지면서 `.task` 가 **취소**된다.
2. `AsyncStream` 이 끝나고 `onTermination` 이 postgres_changes 콜백을 떼어 간다.
3. 그런데 **채널은 `subscribed` 상태로 SDK 캐시에 남는다.**
4. 탭에 돌아와 `supabase.channel("bills-realtime")` 을 다시 부르면 토픽이 같으니
   캐시된 그 채널이 그대로 오고, **이미 구독된 채널에는 콜백을 못 붙인다.**
   SDK 는 경고만 찍고 빈 구독을 돌려준다 (`RealtimeChannelV2._onPostgresChange`).

디버그 빌드면 이 경고가 뜬다 — 이게 보이면 이 증상이다:
```
Cannot add "postgres_changes" callbacks for "realtime:bills-realtime" after `subscribe()`.
```

증상이 가려졌던 이유도 여기 있다. 같은 `.task` 가 `fetchBills()` 를 같이 불렀기 때문에
**탭을 오갈 때는 목록이 맞았다.** 켜 둔 채 가만히 있을 때만 안 들어왔다.

**해결** 구독 태스크와 채널을 `BillViewModel` 이 갖고, 뷰 생명주기와 뗐다.
`subscribeToRealtime()` 은 두 번째 호출부터 아무 일도 안 한다.
백그라운드에서 웹소켓이 끊긴 동안 놓친 변경은 `scenePhase` 가 `.active` 로 돌아올 때
한 번 다시 불러 맞춘다.

**재발 방지** 실시간 구독을 화면의 `.task` 로 되돌리지 않는다. 콜백은 언제나
`subscribe()` **전에** 붙어야 하고, 같은 토픽으로 다시 구독할 일이 생기면
`removeChannel` 로 캐시에서 지운 뒤 새로 만든다.

---

## 2026-08-19 · 새로고침 버튼을 걷어내니 빈 화면에서 새로고침할 방법이 없어졌다

**증상** 헤더의 새로고침 버튼을 없애고 당겨서 새로고침(`.refreshable`)으로 바꿨더니,
목록이 **비어 있을 때** 새로고침이 아예 안 됐다.

**원인** `.refreshable` 은 스크롤되는 것에만 붙는다. 빈 상태는 `EmptyStateView` 한 장이라
스크롤 대상이 없다. 게다가 내용이 화면보다 짧으면 `ScrollView` 라도 기본값으로는
튕기지 않아 당길 수가 없다.

**해결** `DS` 에 `pullToRefresh` 를 넣었다. 빈 상태를 `ScrollView` 로 감싸고
`containerRelativeFrame(.vertical)` 로 화면 높이를 채운 뒤 `scrollBounceBehavior(.always)`
로 튕김을 강제한다. 청구서·계좌부·재정의 빈 화면에 전부 붙였다.

**재발 방지** 버튼 없이 당겨서 새로고침만 두는 화면은 **빈 상태도 당길 수 있는지**를
같이 확인한다 (`DESIGN.md` 1번).

---

## 2026-08-19 · 묶어서 송금은 왜 "같은 사람"만 되나

버그는 아니고, 다시 물어보게 될 제약이라 남긴다.

토스 딥링크 `supertoss://send?bank=&accountNo=&amount=` 는 **수취인 한 명, 금액 하나**만
받는다. 여러 사람에게 한 번에 보내는 스킴은 없다. 그래서 묶음은 같은 사람 안에서만
만들고, 사람이 다르면 앱을 사람 수만큼 열었다 돌아와야 한다 (그건 안 한다).

승인은 `updateStatus(billIds:)` 로 **한 요청**에 처리한다. 건별로 나눠 쏘면 중간에
끊겼을 때 일부만 완료로 남아, 나머지가 다시 청구된 것처럼 보인다.

---

## 2026-08-19 · 청구 폼에서 접수 버튼을 누르고 십수 초를 기다렸다

문서와 이 규칙보다 먼저 한 일이라 뒤늦게 채워 넣는다. 내용은
`worker/README.md` 에 더 자세히 있다.

**증상** — 공개 청구 폼에서 접수 버튼을 누르면 한참 아무 일도 안 일어났다.
회선이 나쁘면 십수 초였고, 그동안 화면에 표시가 없어서 두 번 누르게 된다.

**원인** — 전부 사진이었다. 폰 사진 한 장이 2~5MB 인데 그걸 **버튼을 누른 뒤에야**
올리기 시작했다. 나머지(INSERT·푸시)는 다 합쳐 몇 백 ms 다.

**해결** — 업로드를 **사용자가 기다리는 구간 밖으로** 뺐다.

1. 사진을 고르는 즉시 브라우저에서 축소한다 (긴 변 1600px, JPEG 0.8).
   실측 4.07MB → 306KB. 아이폰 HEIC 도 여기서 JPEG 가 된다.
2. 그 축소본을 `/bill/receipt` 로 **미리** 올린다. 사용자가 항목·금액을 채우는
   동안 끝나므로, 제출은 받아둔 URL 만 넘기는 INSERT 하나로 끝난다.
3. 선행 업로드가 실패하면 축소본을 그대로 붙여 예전 경로(`/bill/submit` 이 직접
   업로드)로 보낸다. JS 가 안 도는 브라우저도 이 경로다. **접수 자체는 항상 된다.**

빨라진 대신 두 가지가 따라왔다.

- **접수까지 안 온 사진이 R2 에 남는다.** 사진을 바꿨을 때와 접수 전에 창을 닫을
  때(`pagehide` + `sendBeacon`) `/bill/receipt/discard` 로 지우지만, 브라우저가
  못 보내고 꺼지는 경우는 못 막는다. 고아 파일을 0 으로 만들려면 R2 수명 주기
  규칙이 필요하고 그건 별개의 일이다.
- **영수증 URL 이 클라이언트를 거쳐 온다.** 그대로 믿으면 아무 주소나 적어 넣어
  관리자 앱이 남의 서버 이미지를 불러오게 만들 수 있고, URL 만 알면 남의 영수증을
  지울 수 있다. `/bill/receipt` 가 URL 에 HMAC-SHA256 서명(`token`)을 찍어 같이
  주고 제출·폐기 때 검증한다. 폐기는 한 겹 더 봐서, 이미 `bills` 에 쓰인
  영수증이면 거절한다.

**재발 방지** — 느리다는 말이 나오면 **무엇이 오래 걸리는지 먼저 재고**, 크게 걸리는
쪽을 줄이거나 기다리는 구간 밖으로 옮긴다 (여기서는 둘 다 했다 — 4MB→300KB, 그리고
제출 전으로). 그리고 어떤 값이든 **클라이언트를 거쳐 돌아오게 만들면 서명을 붙인다.**
서버가 만든 값이라도 다시 받는 순간부터는 남이 적어 넣은 값과 구별이 안 된다.
